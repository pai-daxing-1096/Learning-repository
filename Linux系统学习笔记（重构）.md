硬链接只认inode编号

# Linux系统学习笔记

## 一.Linux简介

### 1.Linux系统架构：

**Linux操作系统=Linux内核+一套系统调用的API接口**

硬件→Linux内核→系统调用接口→shell+函数库→应用程序

1. 操作系统内核直接参考硬件规格写成，所以同一个操作系统程序不能安装在不一样的硬件架构上，主流的架构有 x86 架构和主要用于手机与平板设备的 ARM 架构
2. 操作系统只是管理整个硬件资源，包括 CPU 、内存、输入输出设备及文件系统等。如果没有其他应用程序辅助，操作系统只能是让电脑主机 Ready 而已，并没有其他功能。
3. 应用程序的开发都是参照操作系统提供的 API ，所以该应用程序只能在该操作系统上面运行，不能在其他操作系统上运行，这就是为什么大家在下载应用软件的时候会让你选择是什么平台的安装

---

### 2.Linux快捷键

1. `TAB`   按一次将会补全当前命令，按两次将会列出所有可能
2. `CTRL + C`   立刻终止当前命令
3. `CTRL + D`   退出当前登录
4. `CTRL + L`   清空屏幕
5. `CTRL + A`   使光标跳至最前方

---

---

## 二.Linux的目录结构

### 1.Linux与Windows目录结构的区别

在Linux系统中，路径之间的层级关系，使用`/`来表示

在Windows系统中，路径之间的层级关系，使用`\`来表示d

#### (1)Linux

​	`/usr/local/hello.txt`

注意：

- 开头的`/`表示**根目录**
- 后面的`/`表示层级关系

#### (2)Windows

​	`D:\data\work\hello.txt`

注意：

- `D:`表示D盘
- `\`表示层级关系

---

### 2.文件系统层次结构标准

| 目录   | 作用                                                         |
| ------ | ------------------------------------------------------------ |
| /bin   | 存放**基本用户命令**，如`ls`、`cp`、`cat`等，这些命令在单用户模式或系统启动早期都必须可用，所有用户都可以执行 |
| /boot  | 存储**启动系统所需的文件**，包括Linux内核、初始内存盘、引导加载文程序配置文件 |
| /dev   | 包含**设备文件**，代表设备中的**硬件设备**，如硬盘(`/dev/sda`)、终端(`/dev/tty`)、随机数生成器(`/dev/random`)。应用程序通过读写这些文件来访问硬件 |
| /etc   | 存放**系统配置文件**，几乎所有的**全局配置**都位于此，如网络设置(`/etc/network`)、用户账户信息(`/etc/passwd`)、软件包配置(`/etc/yum.conf`) |
| /home  | **普通用户的个人主目录**所在地，每个用户都有一个子目录(如`/home/pai`)，存放用户的文档、下载、配置(隐藏文件如`.ssh`)等 |
| /lib   | **存储共享库**和**内核模块**，供`/bin`和`/sbin`中的程序调用，也包含某些驱动程序 |
| /media | 作为**可移动介质的挂载点**，如CD/DVD、USB闪存盘、外界硬盘，系统自动挂载时通常在此创建子目录 |
| /mnt   | 用于**临时挂载**其它文件系统(如网络存储、额外分区)，通常由管理员手动挂载，作为临时挂载点 |
| /opt   | 存放可选的**第三方应用程序软件包**，软件通常会以完整目录形式安装于此，避免与系统默认程序冲突 |
| /proc  | **虚拟文件系统**，提供内核与进程的实时信息（存在于内存中）。例如 `/proc/cpuinfo` 查看 CPU 信息，`/proc/<pid>/` 查看进程详情。也可通过写入某些文件（如 `/proc/sys/`）动态修改内核参数。 |
| /root  | **超级管理员的个人主目录**，正常情况下普通用户无权访问，用于储存root用户的个性化配置和重要脚本 |
| /run   | **临时文件系统**，用于储存系统启动以来的运行时数据，如进程PID文件、当前登录会话、服务状态等 |
| /sbin  | 存放**系统管理命令**，通常仅供root用户使用，如`flask`、`mount`、`systemctl`，这些通常命令用于系统维护、启动修复等 |
| /srv   | 存放**本系统提供的服务数据**，即服务（如 HTTP、FTP、RSync）所管理的文件。例如网页服务器可将网站文件放在 `/srv/www/` 下 |
| /sys   | 虚拟文件系统（`sysfs`），**暴露内核对象、设备及驱动信息**，比 `/proc` 更加结构化。用于设备管理和电源管理（如 `/sys/class/`） |
| /tmp   | 存放**临时文件**，任何用户和程序都可以读写此目录，但文件通常不保证持久性 |
| /usr   | **用户系统资源**和**应用程序**的主要存放地，其下的子目录类似根目录（如 `/usr/bin`、`/usr/lib`、`usr/share`），但存放的是非启动必需的程序、库、文档等。大部分软件默认安装于此 |
| /var   | 存放**经常变化的数据**，例如日志文件（`/var/log`）、邮件队列（`/var/mail`）、打印队列（`/var/spool`）、临时缓存（`/var/cache`）、系统运行时的状态文件（`/var/lib`）。 |

---

### 3.相对路径、绝对路径和特殊路径符

#### (1)相对路径和绝对路径

- 绝对路径：以根目录为起点，描述路径的一种写法，路径描述以`/`为开头

	例：`cd /home/itheima/Desktop`

- 相对路径：以当前目录为起点，描述路径的一种写法，路径描述无需以`/`为开头

​	例：`cd Desktop`

#### (2)特殊路径符

- `.`    表示当前目录，比如`cd ./Desktop`表示切换到当前目录下的Desktop目录内，和`cd Desktop`效果一致，其可以与`/`连同使用[^4]
- `..`   表示上一级目录，比如：`cd .. `即可切换到上一级目录，`cd ../..`切换到**上二级目录**，其可以与路径`/`直接连接使用[^4]
- `~`    表示HOME目录，比如：`cd ~`即可切换到HOME目录或`cd ~/Desktop`，切换到HOME内的Desktop目录

---

---

## 三.Linux命令入门

- 命令行：即Linux终端（Terminal)，是一种**命令提示符页面**。以纯**字符**的形式操作系统，可以使用各种字符化命
	令对系统发出操作指令。
- 命令：即Linux程序。一个命令就是一个**Linux的程序**。**命令没有图形化页面**，可以在命令行（终端中）提供字符化的反馈

### 1.Linux命令基础格式

无论是什么命令，用于什么用途，在Linux中，命令有其通用的格式：

  `command [OPTION] [parameter]`

- **command:命令本身**
- **options：[可选，非必填]命令的一些选项，可以通过选项控制命令的行为细节**

> 语法中的`[]`表示可选的意思

示例：

  `ls -l /home/itheima`：

- 其中`ls`是命令本身，`-l`是选项,`/home/itheima`是参数

	- **意思是以列表的形式，显示`/home/itheima`目录内的内容**

	`cp -r test1 test2`：

- 其中`cp`是命令本身，`-r`是选项，`test1`和`test2`是参数

	- **意思是复制文件夹`test1` 成为`test2`**

---

---

## 四.Linux命令

### 1.文件及目录的操作命令

#### (1)ls命令

ls命令的作用是列出目录下的内容

语法：

`ls [OPTION]... [FILE]...`

- `[OPTION]`   选项（可连续使用多个选项）
- `[FILE]`   文件/路径

| OPTION | 作用                                                 | 示例              |
| ------ | ---------------------------------------------------- | ----------------- |
| `-a`   | 表示all的意思，即列出全部文件(包括隐藏的文件/文件夹) | `ls -a /home/pai` |
| `-l`   | 表示以列表的形式展示内容，并展示更多信息             | `ls -l /home/pai` |
| `-h`   | 表示以易于阅读的形式，列出文件大小，如K、M、G        | `ls -h /home/pai` |
| `-i`   | 用于检查文件inode                                    | `ls -i /home/pai` |
| `-t`   | 按照文件修改的时间来排序                             | `ls -t /home/pai` |
| `-R`   | 递归列出子每个子目录及其内容                         | `ls -R /home/pai` |

> 命令可以连续使用，例如：`ls -la /home/pai`

#### (2)cd命令

cd命令可以更改当前的工作目录

语法:

`cd [dir]`

- `dir`   目标目录

| dir  | 作用                 | 示例    |
| ---- | -------------------- | ------- |
| `~`  | 回到用户的Home目录   | `cd ~`  |
| `.`  | 代指当前目录         | `cd .`  |
| `..` | 回到上一级位置       | `cd ..` |
| `-`  | 回到上一次所在的目录 | `cd -`  |

> 若未使用dir则默认回到当前用户的家目录

#### (3)pwd命令

pwd命令可以查看当前所在的工作目录

语法:`pwd`

#### (4)tree命令

展示当前目录的树状图

语法：

`tree [OPTHON]... [FILE]`

- `[OPTION]`   选项（可连续使用多个选项）
- `[FILE]`   文件/路径

| OPTION     | 作用                                               | 示例                  |
| ---------- | -------------------------------------------------- | --------------------- |
| `-L level` | 限制目录遍历最大深度（level为层数）                | `tree -L 2 /home/pai` |
| `-a`       | 显示所有文件，包括隐藏文件                         | `tree -a /home/pai`   |
| `-d`       | 只显示目录                                         | `tree -d /home/pai`   |
| `-f`       | 在每个文件或目录前显示其完整路径（从当前目录开始） | `tree -f /home/pai`   |
| `-h`       | 以人类易读的方式显示文件大小                       | `tree -h /home/pai`   |
| `--du`     | 显示每个子目录的磁盘使用情况                       | `tree --du /home/pai` |
| `-p`       | 打印每个文件的权限信息                             | `tree -p /home/pai`   |

> 在CentOS等系统中需要下载该命令：
>
> `yum install tree -y`

#### (5)mkdir命令

通过mkdir命令可以创建新的目录（文件夹）

语法：

`mkdir [OPTION]... DIRECTORY...`

- `[OPTION]...`   选项（可连续使用多个选项）

- `DIRECTORY...`   创建的目录的名称或路径（可连续指定多个名称或路径）

| OPTION | 作用                                                     | 示例                       |
| ------ | -------------------------------------------------------- | -------------------------- |
| `-p`   | 递归，自动创建不存在的父目录，适用于创建连续多层级的目录 | `mkdir -p /home/pai/hello` |
| `-v`   | 可以直观地看见创建了哪些目录                             | `mkdir -v /home/pai/hello` |

#### (6)touch命令

可以通过touch命令创建文件或修改时间戳

语法：

`touch [OPTION]... FILE...`

- `[OPTION]...`   选项（可连续使用多个选项）
- `FILE...`   创建的文件的名称（可连续指定多个）

| OPTION | 作用                                                | 示例                                 | 结果                                                         |
| ------ | --------------------------------------------------- | ------------------------------------ | ------------------------------------------------------------ |
| `-c`   | 不创建任何文件                                      | `touch -c hello.txt`                 | 如果hello.txt文件不存在，则静默跳过，不创建新文件，如果hello.txt文件存在，则正常更新时间戳 |
| `-a`   | 仅更新访问时间（atime），同时隐式更新 ctime         | `touch -a hello.txt`                 | 刷新hello.txt文件的最近访问时间和最近改动时间                |
| `-m`   | 仅更新修改时间（mtime），同时隐式更新 ctime         | `touch -m hello.txt`                 | 刷新hello.txt文件的最近更改和最近改动的时间                  |
| `-t`   | 修改时间戳至指定时间（默认同时更新 atime 和 mtime） | `touch -t 202604132213.12 hello.txt` | 将hello.txt文件的最近访问时间和最近改动时间改为2026-04-13-22:13:12 `[[CC]YY]MMDDhhmm[.ss]` |

#### (7)file命令

输出文件的类型，例如ASCII text，表示这是一个ASCII编码的文本文件。

语法：

`file [OPTION]... FILE...`

- `[OPTION]...`   选项（可连续使用多个选项）
- `FILE...`   需要查看的文件的名称（可连续指定多个）

| OPTION | 作用                                     | 示例                | 结果                                                         |
| ------ | ---------------------------------------- | ------------------- | ------------------------------------------------------------ |
| `-b`   | 仅显示文件类型，不显示文件名             | `file -b hello.txt` | 只显示文件类型，没有`hello.txt:`前缀                         |
| `-f`   | 从指定的文件中读取文件名列表，并逐一检查 | `file -f list.txt`  | 读取文件名列表`list.txt`中的数据后识别文件名列表中的所有文件的文件类型 |
| `-i`   | 显示文件的MIME类型                       | `file -i hello.txt` | 识别hello.txt文件的MIME类型，这对于网络传输非常有用          |
| `-z`   | 尝试读取压缩文件的内容再进行查询         | `file -z hello.gz`  | 解压并识别hello.gz后输出内部真实文件类型                     |

#### (8)stat命令

检查文件的属性

语法：

`stat [OPTION]... FILE...`

- `[OPTION]...`   选项（可连续使用多个选项）
- `FILE...`   需要查看的文件的名称（可连续指定多个）

| OPTION      | 作用             | 示例                      | 结果               |
| ----------- | ---------------- | ------------------------- | ------------------ |
| `-c format` | 允许输出指定格式 | `stat -c format file.txt` | 以指定格式输出信息 |

| format | 含义                        | 示例输出                              |
| :----- | :-------------------------- | :------------------------------------ |
| `%n`   | 文件名                      | `download.sh`                         |
| `%s`   | 文件大小（字节）            | `2003`                                |
| `%F`   | 文件类型                    | `regular file`                        |
| `%a`   | 权限（八进制）              | `664`                                 |
| `%A`   | 权限（符号形式）            | `-rw-rw-r--`                          |
| `%U`   | 所有者用户名                | `pai`                                 |
| `%G`   | 所有者组名                  | `pai`                                 |
| `%u`   | 所有者 UID                  | `1000`                                |
| `%g`   | 所有者 GID                  | `1000`                                |
| `%i`   | inode 号                    | `786471`                              |
| `%h`   | 硬链接数                    | `1`                                   |
| `%y`   | 最近修改时间（完整）        | `2026-04-13 20:03:43.454613676 +0800` |
| `%Y`   | 最近修改时间（Unix 时间戳） | `1748275423`                          |
| `%X`   | 最近访问时间（Unix 时间戳） | `...`                                 |
| `%Z`   | 最近改动时间（Unix 时间戳） | `...`                                 |
| `%w`   | 文件创建时间（可能为空）    | `2026-04-13 20:03:43.454613676 +0800` |

演示：

```shell
[pai@AliYunLinux ~]$ stat download.sh 
  文件：download.sh
  大小：2003            块：8          IO 块：4096   普通文件
设备：fd03h/64771d      Inode：786471      硬链接：1
权限：(0664/-rw-rw-r--)  Uid：( 1000/     pai)   Gid：( 1000/     pai)
最近访问：2026-04-19 23:29:15.421877660 +0800	#最近一次访问文件的时间
最近更改：2026-04-13 20:03:43.454613676 +0800	#最近一次修改内容的时间
最近改动：2026-04-13 20:03:43.456613704 +0800    #最近一次修改内容或属性的时间，内容和属性发生变化都会导致此时间戳改变
创建时间：2026-04-13 20:03:43.454613676 +0800
```

> **inode 是什么**：文件元数据（大小、权限、时间戳、数据块位置），文件名不在 inode 里

#### (9)cp命令

用于复制文件\文件夹

语法：
 `cp [OPTION]... SOURCE... DIRECTORY`

- `[OPTION]...`   选项（可连续使用多个选项）
- `SOURCE...`   源文件路径（可连续指定多个文件）
- `DIRECTORY`   目标目录（必须已经存在，否则会报错）

| OPTION      | 作用                                                         | 示例                           | 结果                                                         |
| ----------- | ------------------------------------------------------------ | ------------------------------ | ------------------------------------------------------------ |
| `-i`        | 覆盖前先询问用户（y确认）                                    | `cp -i a.txt b.txt`            | a.txt的内容被复制到b.txt中，且复制前会先进行确认             |
| `-a`        | 复制时保留所有属性                                           | `cp -a a.txt b.txt`            | b.txt文件继承a.txt文件的所有属性（包括 xattr、ACL、所有权、时间戳等） |
| `-f`        | 不询问，直接覆盖（会覆盖 -i 的效果）                         | `cp -f a.txt b.txt`            | a.txt的内容被复制到b.txt中，且复制前不会进行确认             |
| `-n`        | 如果目标文件已存在，则不进行复制                             | `cp -n a.txt b.txt`            | 因为b.txt文件存在，所有a.txt的内容没有被复制到b.txt中        |
| `-u`        | 只有当源文件比目标文件新，或目标不存在时才复制（比较的是 mtime 而非内容） | `cp -u a.txt b.txt`            | 因为a.txt文件修改时间比b.txt文件更晚（更新），所以a.txt文件的内容被复制到了b.txt文件内 |
| `-v`        | 打印每个复制操作的信息                                       | `cp -v a.txt b.txt`            | 显示过程                                                     |
| `-r` / `-R` | 复制目录及其内部所有内容（两者基本等价）                     | `cp -r ~/hello/ ~/world/`      | 将 hello 目录复制到 world 目录下（即 ~/world/hello/）        |
| `-T`        | 强制把目标（不是源文件，是目标位置）当作普通文件处理。如果目标是一个已存在的目录，则报错（防止意外将文件复制到目录内部） | `cp -T a.txt /home/pai/hello/` | 报错，因为hello/是一个目录                                   |
| `-t`        | 多文件复制                                                   | `cp -t ~/hello/ a.txt b.txt`   | 将a.txt文件和b.txt文件都复制到~/hello/下                     |
| `-p`        | 保留原始文件的修改时间、访问时间、权限、所有者等             | `cp -p a.txt ~/hello/`         | 将a.txt文件复制到~/hello/下，但是修改时间、访问时间、权限、所有者等没有变化 |

| 类型\情况 | 存在                         | 不存在                 |
| --------- | ---------------------------- | ---------------------- |
| 文件      | 将源文件内容复制到指定文件中 | 创建指定文件后复制进去 |
| 目录      | 将源文件复制到指定目录中     | 报错                   |

> [!WARNING]
>
> 复制目录时**必须加 `-r` 或 `-R`**

#### (10)mv命令

用于移动文件\文件夹

语法：`mv [OPTION]... SOURCE... DIRECTORY`

- `[OPTION]...`   选项（可连续使用多个选项）
- `SOURCE...`   源文件路径（可连续指定多个文件）
- `DIRECTORY`   目标目录（必须已经存在，否则会报错）

| OPTION | 作用                                                         | 示例                          | 结果                                                         |
| ------ | ------------------------------------------------------------ | ----------------------------- | ------------------------------------------------------------ |
| `-i`   | 在覆盖目标文件前提示用户确认（y确认）                        | `mv -i a.txt c.txt`           | 因为c.txt文件不存在，所以将a.txt文件改名为c.txt              |
| `-f`   | 不询问，直接覆盖目标文件（会覆盖 -i 的效果）                 | `mv -f a.txt b.txt`           | 将a.txt文件的名称重命名为b.txt，同时a.txt文件消失            |
| `-n`   | 如果目标文件已存在，则不会进行移动操作                       | `mv -n a.txt b.txt`           | 因为b.txt文件存在，所以没有进行任何操作                      |
| `-u`   | 只有当源文件比目标文件更新（修改时间），或目标文件不存在时，才进行移动 | `mv -u a.txt b.txt`           | 因为a.txt文件修改时间比b.txt文件更晚（更新），所以a.txt文被重命名为b.txt，同时a.txt文件消失 |
| `-v`   | 打印每个移动操作的详细信息                                   | `mv -v a.txt b.txt`           | 显示过程                                                     |
| `-b`   | 如果目标文件已存在，会在覆盖前自动为其创建一个备份（备份文件默认添加 `~` 后缀） | `mv -b a.txt b.txt`           | 因为b.txt文件存在，所以会先将b.txt文件重命名为b.txt~，再将 a.txt文件重命名为b.txt，a.txt文件消失 |
| `-t`   | 可将目标目录写在前面，方便多文件移动（源文件可放在后面）     | `mv -t ~/hello/ a.txt b.txt ` | 将a.txt文件和b.txt文件都移动到~/hello/目录下                 |
| `-T`   | 将目标当作普通文件，而非目录。如果目标是一个已存在的目录，则报错 | `mv -T a.txt ~/hello/`        | 报错                                                         |
| `-S`   | 与 -b或连用，指定备份文件的后缀（默认是~）                   | `mv -b -S .bak a.txt b.txt`   | 将a.txt文件的内容覆盖至到b.txt文件中，如果b.txt文件存在，会先将b.txt文件备份为b.txt.bak文件再移动 |

| 类型\情况 | 存在                                             | 不存在                   |
| --------- | ------------------------------------------------ | ------------------------ |
| 文件      | 覆盖目标文件内容但不改变目标文件名称，原文件消失 | 先创建指定文件再覆盖内容 |
| 目录      | 将文件移动至目标目录内                           | 报错                     |

> [!CAUTION]
>
> `-i`、`-f`、`-n` 三个选项互斥，如果同时指定，只有最后一个选项生效
>
> 跨文件系统移动文件时，`mv` 实际上会执行“先复制后删除”的操作，这会导致文件的索引节点（inode）编号发生改变，速度也会比同分区内移动慢不少，因为同一文件系统内 `mv` 只是改目录项，不涉及数据移动；跨文件系统需要复制+删除，所以 inode 会变，且拷贝非原子，且中途断电可能造成文件半拷贝
>
> 每个文件系统（比如 `/dev/sda1` 上的 ext4、`/dev/sdb1` 上的 xfs）都有自己独立的 inode 表。inode 号 786471 只在它所属的文件系统里有意义，另一个文件系统里可能已经有自己的 inode 786471，指向完全不同的文件。

#### (11)rm命令

用于删除文件\文件夹

语法：`rm [OPTION]... [FILE]...`

- `[OPTION]...`   选项（可连续使用多个选项）
- `[FILE]...`   需要删除的文件的名称（可连续指定多个）

| OPTION     | 作用                                                   | 示例             | 结果                          |
| ---------- | ------------------------------------------------------ | ---------------- | ----------------------------- |
| `-i`       | 删除前逐一询问确认（y确认）                            | `rm -i a.txt`    | 删除a.txt文件，删除前提示     |
| `-f`       | 忽略不存在的文件，不提示确认（会覆盖 -i）              | `rm -f a.txt`    | 删除a.txt文件，删除前没有提示 |
| `-r`或`-R` | 删除目录及其内部所有内容（**删除目录时必须加此参数**） | `rm -r ~/hello/` | 删除hello/目录                |
| `-v`       | 打印每个删除操作的过程                                 | `rm -v a.txt`    | 显示过程                      |

> [!CAUTION]
>
> `rm` 默认**不删除目录**，必须加 `-r` 才能删除目录。
>
> `-f` 会抑制错误信息（比如文件不存在时不报错）且不询问确认。

---

### 2.查找命令

#### (1)which命令

查看所使用的一系列命令的程序文件存放在哪里

语法：

`which command`

#### (2)find命令

用于搜索指定文件（`find` 是**递归**的，会遍历起始路径下的所有子目录）

语法：

`find [path...] [expression...]`

- `[path...]`   起始路径（可连续使用多个）
- `[expression]`   表达式（包含测试条件和动作）
	- 表达式可以包含测试条件（如 `-type`、`-name`）和动作（如 `-print`、`-delete`、`-exec`），默认动作为 `-print`

| action    | 作用               | 示例                                                         |
| --------- | ------------------ | ------------------------------------------------------------ |
| `-print`  | 默认动作           | `find /var/log -name "*.log"`                                |
| `-delete` | 直接删除           | `find /tmp -type f -mtime +7 -delete`                        |
| `-exec`   | 对每个结果执行命令 | `find /path -name "*.log" -exec command {} \;`<br />`find /path -name "*.log" -exec command +` |

- `command`   命令
- `{}` 是匹配结果的占位符
- `\;` 表示命令结束（反斜杠转义分号）
- `+`   表示命令结束

> [!IMPORTANT]
>
> `-exec ... {} \;`指每个文件进行一次操作，而`-exec ... {} +`会对所有文件进行一次操作（或分几批）

> 注意：`-delete` 会自动忽略目录，且不会删除非空目录。它是 GNU find 的扩展，不是 POSIX 标准，但现代 Linux 都支持

##### ①按类型查找

语法：

`find [path...] -type <类型字符>`

- `[path...]`   起始路径
- `-type`   使用类型查找
- `<类型字符>`   指定要查找的类型

| 类型字符 | 含义             |
| :------- | :--------------- |
| `f`      | 普通文件         |
| `d`      | 目录             |
| `l`      | 符号链接         |
| `b`      | 块设备文件       |
| `c`      | 字符设备文件     |
| `s`      | socket 文件      |
| `p`      | 管道文件（FIFO） |

##### ②按名称查找

语法：

`find [path...] -name "通配符"`

- `[path...]`   起始路径（可连续使用多个）
- `-name`

| 通配符               | 含义                           | 示例                                                       |
| :------------------- | :----------------------------- | :--------------------------------------------------------- |
| `*`                  | 匹配任意字符串（包括空串）     | `*.log` 匹配所有以 `.log` 结尾的文件                       |
| `?`                  | 匹配任意单个字符               | `file?.txt` 匹配 `file1.txt`、`filea.txt`                  |
| `[abc]`              | 匹配方括号中的任意一个字符     | `file[123].txt` 匹配 `file1.txt`、`file2.txt`、`file3.txt` |
| `[a-z]`              | 匹配指定范围内的任意一个字符   | `file[a-c].txt` 匹配 `filea.txt`、`fileb.txt`、`filec.txt` |
| `[!abc]` 或 `[^abc]` | 匹配不在方括号中的任意一个字符 | `file[!0-9].txt` 匹配不以数字结尾的文件                    |

> [!WARNING]
>
> 注意：`-name` 只匹配文件名，不匹配路径。如果要匹配整个路径，用 `-path`：
>
> ```shell
> find / -path "*/backup/*.tar.gz"
> ```

##### ③按时间查找

语法：

`find [path...] 时间选项 [+|-]时间`

- `[path...]`   起始路径（可连续使用多个）
- `[+|-]`   大于|小于

| 时间选项  | 含义                                   | 选项               |
| :-------- | :------------------------------------- | :----------------- |
| **mtime** | 修改时间（内容改变）                   | `-mtime` / `-mmin` |
| **atime** | 访问时间（被读取）                     | `-atime` / `-amin` |
| **ctime** | 改动时间（元数据改变，如权限、所有者） | `-ctime` / `-cmin` |

语法规则（以 `-mtime` 为例）：

| 写法        | 含义                                     |
| :---------- | :--------------------------------------- |
| `-mtime +7` | 修改时间在 **7 天前**（严格大于 7 天）   |
| `-mtime -7` | 修改时间在 **7 天内**（小于 7 天）       |
| `-mtime 7`  | 修改时间在 **恰好 7 天前的那 24 小时**内 |

> `-mmin` 同理，只是单位是分钟。`-mmin +60` 表示 60 分钟前修改的

##### ④按大小查找

语法：
`find [path...] -size [+|-]n[单位]`

- `[path...]`   起始路径（可连续使用多个）
- `n`   数字

| 单位 | 含义            | 示例           |
| :--- | :-------------- | :------------- |
| `c`  | 字节            | `-size +1000c` |
| `k`  | KB（1024 字节） | `-size +10k`   |
| `M`  | MB              | `-size +100M`  |
| `G`  | GB              | `-size +1G`    |

> 注意：`+100M` 表示**严格大于** 100MB，不包括恰好 100MB 的文件
>
> `+` 表示大于，`-` 表示小于，不加符号表示恰好等于（严格来说，`-size 100M` 匹配大小在 100M 到 100M+1 块之间的文件（约等于 100M，但受块大小影响）

##### ⑤按权限查找

语法：

`find [path...] -perm [-|/]权限的数字表示法`

- `[path...]`   起始路径（可连续使用多个）

- `-`   至少包含指定的权限
- `/`   包含指定的权限中任意一个权限位

示例：

```shell
find /path -perm 644          # 权限恰好是 644
find /path -perm -644         # 至少包含 644 的权限（owner 有 rw，group 有 r，other 有 r）
find /path -perm /644         # 包含 644 中任意一个权限位
```

##### ⑥按所有者/组查找

语法：

`find [path...] -user username`

`find [path...] -group groupname`

> 按所有者/组

`find [path...] -nouser`

`find [path...] -nogroup`

> 查找没有所有者的文件（可能是用户被删除后遗留的）

- `[path...]`   起始路径（可连续使用多个）

- `username`   用户名
- `groupname`   组名

##### ⑦逻辑组合

示例：

```shell
# 与（默认就是与）
find /var/log -name "*.log" -a -mtime +7   # -a 可以省略，但加上更清晰

# 或
find /path -name "*.log" -o -name "*.txt"

# 非
find /path -name "*.log" ! -name "nginx*"
find /path -name "*.log" -not -name "nginx*"

# 括号分组（注意括号要转义或引号）
find /path \( -name "*.log" -o -name "*.tmp" \) -mtime +30
```

##### ⑧额外常用选项

**配合xargs**

示例：

```shell
find /var/log -name "*.log" -mtime +30 | xargs rm -f
```

**`-exec` 和 `xargs` 的区别**：

- `-exec` 是 `find` 的内置功能，直接调用命令
- `xargs` 是管道传递，可以更灵活地控制并发、批量大小

***限制搜索深度***

限制搜索深度，避免递归太深或只搜索顶层

示例：

```shell
find /var/log -maxdepth 1 -name "*.log"   # 只在 /var/log 本身这一层找
find /data -mindepth 2 -type f            # 从第二层开始找文件
```

**使用正则表达式**

示例：

```shell
find /etc -regex ".*\.conf$"              # 匹配所有以 .conf 结尾的路径
find /var/log -iregex ".*error.*\.log"    # 不区分大小写
```

**排除某些目录不搜索**

示例：

```shell
find / -path /proc -prune -o -type f -name "*.conf" -print
          指定路径
```

**按文件修改时间与指定文件或时间点比较**

示例：

```shell
find /app -newer /tmp/reference_file      # 比 reference_file 新的文件
find /app -newermt "2026-08-01"           # 在 2026-08-01 之后修改的文件
```

**查找空文件或空目录**

示例：

```shell
find /tmp -type f -empty                 # 空文件
find /tmp -type d -empty                 # 空目录
```

##### ⑨进阶部分

| 选项        | 用途                           |
| :---------- | :----------------------------- |
| `-printf`   | 自定义输出格式                 |
| `-inum`     | 按 inode 号查找                |
| `-samefile` | 查找指向同一 inode 的文件      |
| `-links`    | 按硬链接数查找                 |
| `-ok`       | 交互式执行命令（删除前确认）   |
| `-depth`    | 先处理目录内容，再处理目录本身 |
| `-xdev`     | 不跨文件系统搜索               |

##### 案例

1. 处理文件名含空格的问题：

```shell
# 错误：文件名含空格会出问题
find /path -type f | xargs rm

# 正确：用 -print0 和 -0 配合
find /path -type f -print0 | xargs -0 rm
```

> [!CAUTION]
>
> **`-exec` 和 `xargs` 的区别**：
>
> `-exec` 是 `find` 的内置功能，直接调用命令
>
> `xargs` 是管道传递，可以更灵活地控制并发、批量大小

---

### 3.文件内容处理命令

#### (1)wc命令

统计文件的行数、单词数量等

语法：

`wc [OPTION]... [FILE]...`

- `[OPTION]...`   选项（可连续使用多个选项）
- `FILE...`   需要查看的文件的名称（可连续指定多个）

| OPTION | 作用                               | 示例             | 结果                               |
| ------ | ---------------------------------- | ---------------- | ---------------------------------- |
| `-c`   | 统计字节数量                       | `wc -c file.txt` | 统计了file.txt文件的字节数量       |
| `-m`   | 统计字符数量                       | `wc -m file.txt` | 统计了file.txt文件的字符数量       |
| `-l`   | 统计行数                           | `wc -l file.txt` | 统计了file.txt文件的行数           |
| `-w`   | 统计单词数量                       | `wc -w file.txt` | 统计了file.txt文件的单词数量       |
| `-L`   | 现实最长行的长度（最长行的字节数） | `wc -L file.txt` | 统计了file.txt文件的中最长行的长度 |

> [!NOTE]
>
> `wc -l` 统计的是**换行符的数量**，不是“行数”本身
>
> **自动添加换行符（人类友好）**：`echo`、`ls`、`head`、`tail`、`grep`、`awk print`、`wc`、`ps`、`df`、`free`、`ip`、`ss`、Python `print()`、Perl `say`。
>
> **不添加换行符（机器友好/原始透传）**：`printf`、`cat`（取决于源文件）、`tr`、`dd`、`awk printf`、Python `sys.stdout.write()`、Perl `print`。

#### (2)tail命令

查看文件尾部内容，并且监控文件新增内容

语法：

` tail [OPTION]... [FILE]...`

- `[OPTION]...`   选项（可连续使用多个选项）
- `FILE...`   需要查看的文件的名称（可连续指定多个）

| OPTION    | 作用                                                  | 示例                   | 结果                                         |
| --------- | ----------------------------------------------------- | ---------------------- | -------------------------------------------- |
| `-f`      | 持续监控跟踪，但文件被轮转/删除后会停止跟踪           | `tail -f file.txt`     | 监控file.txt文件尾部10行的内容（只显示10行） |
| `-F`      | 持续监控跟踪，文件被轮转/删除后会自动重新打开同名文件 | `tail -F file.txt`     | 监控file.txt文件尾部10行的内容（只显示10行） |
| `-n num`  | 表示查看尾部多少行（默认10行）                        | `tail -n 20 file.txt`  | 显示file.txt文件尾部20行的内容（只显示20行） |
| `-n +num` | 从第 num 行开始显示到文件末尾                         | `tail -n +20 file.txt` | 显示file.txt文件从第20行显示到尾行的内容     |

> `tail -f` 通过定期检查文件大小变化来发现新增内容，而不是每次重读整个文件
> `tail -F` 比 `-f` 更鲁棒，能在日志轮转后继续跟踪新文件

#### (3)head命令

查看文件头部内容

语法：

`head [OPTION]... [FILE]...`

- `[OPTION]...`   选项（可连续使用多个选项）
- `FILE...`   需要查看的文件的名称（可连续指定多个）

| OPTION    | 作用                          | 示例                   | 结果                                |
| :-------- | :---------------------------- | :--------------------- | ----------------------------------- |
| `-n num`  | 显示开头多少行（默认10行）    | `head -n 20 file.txt`  | 显示file.txt文件开头前20行的内容    |
| `-c num`  | 显示开头多少字节              | `head -c 100 file.txt` | 显示file.txt文件开头前100字节的内容 |
| `-n -num` | 显示除最后 num 行外的所有内容 | `head -n -5 file.txt`  | 显示file.txt文件除了最后5行的内容   |

#### (3)cat命令

可以通过cat命令查看内容

语法：

`cat [OPTION]... FILE...`

- `[OPTION]...`   选项（可连续使用多个选项）
- `FILE...`   需要查看的文件的名称（可连续指定多个）

| OPTION | 作用         | 示例               | 结果                                            |
| ------ | ------------ | ------------------ | ----------------------------------------------- |
| `-n`   | 显示行号     | `cat -n hello.txt` | 为每一行前添加行号                              |
| `-A`   | 显示所有符号 | `cat -A hello.txt` | 显示出文件内的所有符号（$、^I等会被隐藏的符号） |

#### (4)less命令

同样可以查看文件内容，如果文件过多，将会一页一页地展示，除此之外，还有向上翻页的功能

语法：

`less FILE...`

- `FILE...`   需要查看的文件的名称（可连续指定多个）

| 操作介绍            | 作用         |
| ------------------- | ------------ |
| `[space]`           | 向下翻一页   |
| `b`                 | 向上翻一页   |
| `Enter`             | 翻一行       |
| `q`                 | 退出         |
| `/<关键词>`         | 搜索指定内容 |
| `Page Up/Page Down` | 逐行滚动     |

> 可以使用延伸版`zless`直接查看被.gz打包的文件

---

### 4.输入输出控制

#### (1)管道符

可以将管道符`|`左边的命令的标准输出，以字节流的方式传给右边命令的标准输入

语法：`command1 | command2`

- 过程为线性处理方式，所以可以连续使用

##### 案例

1. 管道默认返回最后一个命令的退出码

```
cmd1 | cmd2
# 整个管道的退出码是 cmd2 的，cmd1 失败时管道仍然成功
# 这会导致脚本漏判错误
```

​	SRE 常用 `set -o pipefail` 来解决：

```
set -o pipefail
cmd1 | cmd2
# 如果 cmd1 或 cmd2 任一失败，整个管道返回非零
```

#### (2)重定向符

- `>`   将左边命令的结果，覆盖写入到右边的文件中
- `>>`   将左边命令的结果，追加写入到右边的文件中
- `2>`   重定向错误输出
- `2>&1`   把 stderr 合并到 stdout（将错误输出合并至标准输出）
- `&>`   同时重定向 stdout 和 stderr（bash 特有）

> [!IMPORTANT]
>
> 管道默认只传 stdout，不传 stderr（`cmd 2>&1 | grep` 才能让 grep 看到错误）
>
> 管道右侧在子shell中执行，变量赋值不生效（`echo "hello" | read var`，`$var` 为空）

##### 案例

1. 重定向符，`2>&1` 的顺序问题

```shell
# 正确：stderr 合并到 stdout，然后一起重定向到文件
cmd > file.log 2>&1

# 错误：stdout 重定向到 file.log 后，stderr 被合并到"当前 stdout"（即终端），文件里没有错误（stderr被定向到了stdout原来的输出，也就是屏幕，导致没有进文件）
cmd 2>&1 > file.log
```

> `2>&1` 必须放在 `>` 后面，顺序不能反。`2>&1 > file` 表示先把 stderr 指向终端，再把 stdout 指向文件，最终 stderr 不会进入文件

2. 子shell变量不生效

```shell
# 错误：var 在子shell里赋值，父shell看不到
echo "hello" | read var
echo $var   # 输出为空

# 正确：用 $() 或 <<< 捕获
var=$(echo "hello")
echo $var   # 输出 hello
```

#### (3)echo命令

在命令行中输出指定内容

语法：`echo [OPTION]... [STRING]...`

- `[OPTION]...`   选项（可连续使用多个选项）
- `[STRING]...`   字符串（可连续使用多个）

| OPTION       | 作用                          |
| :----------- | :---------------------------- |
| **默认行为** | 输出内容后**自动添加换行符**  |
| `-n`         | 取消末尾换行符                |
| `-e`         | 启用转义字符（如 `\n`、`\t`） |

> [!IMPORTANT]
>
> `echo -e` 只在 **Bash** 中有效。Dash（Ubuntu/Debian 默认 `/bin/sh`）不支持，`\n` 不会被解释。在脚本中统一使用 `printf`

> 如果`[STRING]...`过于复杂(包含`\`等特殊符号)，则需使用**双引号**`""`包围`[STRING]...`
>
> 若希望输出命令将会执行的结果，则需使用` $() `包围命令

#### (4)printf命令

在命令行中输出指定内容

语法：

`printf [-v var] format [arguments]`

- `-v var`   赋值给变量
- `format`   格式字符串
- `arguments`   提供给格式字符串中占位符的参数，按顺序填入

> `-v var` 将输出赋值给变量 `var`，赋值后不打印到终端（stdout）
>
> ```shell
> printf -v var "%s %s" "hello" "world"
> echo "$var"
> # 输出：hello world
> ```

| 说明符 | 含义                  | 示例                  | 结果       |
| :----- | :-------------------- | :-------------------- | :--------- |
| `%s`   | 字符串                | `printf "%s" "hello"` | `hello`    |
| `%d`   | 十进制整数            | `printf "%d" 42`      | `42`       |
| `%f`   | 浮点数（默认6位小数） | `printf "%f" 1.5`     | `1.500000` |
| `%x`   | 十六进制整数          | `printf "%x" 255`     | `ff`       |
| `%o`   | 八进制整数            | `printf "%o" 10`      | `12`       |
| `%%`   | 输出百分号 `%`        | `printf "100%%"`      | `100%`     |

带修饰符的格式说明符（宽度、精度、对齐）

语法：`%[flags][width][.precision]conversion`

| 修饰符          | 作用                              | 示例                  | 结果                      |
| :-------------- | :-------------------------------- | :-------------------- | :------------------------ |
| `-`             | 左对齐（默认右对齐）              | `printf "%-5s" "ab"`  | `ab`（左对齐，补3个空格） |
| `0`             | 数字补零                          | `printf "%05d" 42`    | `00042`                   |
| 数字（宽度）    | 最小输出宽度                      | `printf "%10s" "hi"`  | `hi`（右对齐，补8个空格） |
| `.数字`（精度） | 浮点数小数位数 / 字符串最大字符数 | `printf "%.2f" 1.234` | `1.23`                    |

> [!IMPORTANT]
>
> 不自动换行：`printf` 不会像 `echo` 一样自动添加换行符。需要换行必须在 `format` 末尾显式写 `\n`。
>
> 参数不足：如果 `arguments` 数量少于占位符数量，缺失的参数按空字符串或 0 处理（取决于转换类型）。
>
> 参数多余：如果 `arguments` 多于占位符数量，格式字符串会**重复使用**，直到所有参数耗尽。
>
> ```shell
> printf "%s\n" a b c
> # 输出：
> # a
> # b
> # c
> ```
>
> `format` 重用：如果 `format` 被重复使用，每次都会从头开始解释。
>
> `%%` 输出百分号：如果想要输出 `%`，必须写 `%%`，否则 `%` 后面会被当作格式说明符

`echo `vs `printf `对比表

| 命令               | 自动换行 | 支持转义       | 适用场景           |
| :----------------- | :------- | :------------- | :----------------- |
| `echo "text"`      | ✅        | ❌（需 `-e`）   | 简单提示信息       |
| `echo -e "text\n"` | ✅        | ✅（依赖shell） | 不推荐在脚本中使用 |
| `printf "text\n"`  | ❌        | ✅（天然支持）  | **脚本首选**       |

---

### 5.vi\vim编辑器

#### (1)vi命令

语法：`vi [OPTION]... FILE...`

- `[OPTION]...`   选项（可连续使用多个选项）
- `FILE...`   文件名（可连续指定多个文件）

> 因为vi编辑器和vim编辑器的基础命令通用，且vim编辑器相较于vi编辑器更加强大，故此处不对vi编辑器作过多解释
>
> 但CentOS 7 最小化安装可能只有 `vi`（vim-minimal），功能少。建议 `yum install vim -y` 后统一用 `vim`。但生产环境如果只有 `vi`，所以基本操作必须会

#### (2)vim命令

语法：

`vim [OPTION]... FILE...`

- `[OPTION]...`   选项（可连续使用多个选项）
- `FILE...`   文件名（可连续指定多个文件）

**三种模式**

| 模式         | 进入方式                 | 作用                       | 怎么退出                                     |
| :----------- | :----------------------- | :------------------------- | :------------------------------------------- |
| 命令模式     | 启动 vim 后默认          | 移动光标、复制、删除、撤销 | 按 `i`/`a`/`o` 进插入模式，按 `:` 进底线模式 |
| 插入模式     | 命令模式下按 `i`/`a`/`o` | 输入文本                   | `Esc` 退回命令模式                           |
| 底线命令模式 | 命令模式下按 `:`         | 保存、退出、查找替换、设置 | 执行完自动回命令模式，按 `Esc` 取消          |

**命令模式常用键**

##### ①移动

| 键              | 作用           | 备注       |
| :-------------- | :------------- | :--------- |
| `h`/`j`/`k`/`l` | 左/下/上/右    | 替代方向键 |
| `0`             | 行首           |            |
| `$`             | 行尾           |            |
| `gg`            | 文件第一行     |            |
| `G`             | 文件最后一行   |            |
| `w`             | 下一个单词开头 |            |
| `b`             | 上一个单词开头 |            |
| `Ctrl + f`      | 向下翻页       |            |
| `Ctrl + b`      | 向上翻页       |            |

##### ②编辑

| 键         | 作用               | 备注                                |
| :--------- | :----------------- | :---------------------------------- |
| `i`        | 光标前插入         |                                     |
| `a`        | 光标后插入         |                                     |
| `I`        | 行首插入           |                                     |
| `A`        | 行尾插入           |                                     |
| `o`        | 下方新建一行并插入 |                                     |
| `O`        | 上方新建一行并插入 |                                     |
| `x`        | 删除光标处字符     | 相当于 Delete                       |
| `dd`       | 剪切当前行         | 不是删除！`p` 能粘贴回来            |
| `yy`       | 复制当前行         |                                     |
| `p`        | 粘贴到光标下方     |                                     |
| `P`        | 粘贴到光标上方     |                                     |
| `u`        | 撤销               |                                     |
| `Ctrl + r` | 重做               |                                     |
| `.`        | 重复上一次操作     | 超好用，改一处后按 `.` 自动改下一处 |

##### ③底线命令模式常用命令

| 命令            | 作用                         |
| :-------------- | :--------------------------- |
| `:w`            | 保存                         |
| `:q`            | 退出                         |
| `:wq` 或 `:x`   | 保存并退出                   |
| `:q!`           | 不保存强制退出               |
| `:w!`           | 强制保存（只读文件用）       |
| `:set nu`       | 显示行号                     |
| `:set nonu`     | 取消行号                     |
| `:set hlsearch` | 搜索高亮                     |
| `:/关键词`      | 搜索，`n` 下一个，`N` 上一个 |
| `:%s/旧/新/g`   | 全文替换                     |
| `:行号`         | 跳到指定行，如 `:20`         |

##### ④`~/.vimrc` 配置文件

每次打开 vim 都要手动 `:set nu`，太麻烦。可以写一个配置文件：

```shell
vim ~/.vimrc
```

内容：

```shell
set nu          		# 显示行号
set hlsearch    		# 搜索高亮
set pastetoggle=<F2>    # 按下F2后直接开启粘贴模式
```

> [!CAUTION]
>
> 如果你在vim中进行了误操作或者忘了自己刚才在干嘛，**直接使用`q!`不保存退出！！！**（如果因为偷懒而`wq`导致误操作我给你🐎杀了）
>
> 绝对不能使用vi或vim编辑`/etc/sudoers`，必须使用`visudo`进行修改

>  vim 配置文件里 `#` 不是注释符，vim 会把整行当成命令，看到 `#` 会报错

#### 案例

1. `.swp` 文件：vim 的“未保存草稿”

​	你编辑文件时 vim 会自动生成一个 `.文件名.swp` 交换文件

​	如果 vim 异常退出（比如 SSH 断开），下次打开会提示：

```shell
Swap file ".xxx.swp" already exists!
[O]pen Read-Only  [E]dit anyway  [R]ecover  [D]elete it
```

​	**选 R** 恢复上次未保存的内容

​	**选 D** 删除交换文件，正常打开

​	如果不想看到这个提示，直接 `rm .xxx.swp` 再重新打开

2. 自动缩进导致的混乱格式

​	在云服务器上改配置时，大概率会从本地复制一段配置，然后在 vim 里右键粘贴。结果就是：

​	缩进越来越深
​	格式全乱
​	每行前多出一堆空格

​	**原因**：vim 默认有自动缩进，粘贴时会“帮你”再缩进一次。

​	**解决**：粘贴前在底线模式输入：

```shell
# 开启粘贴模式
:set paste
```

​	粘贴完再：

```shell
# 关闭粘贴模式
:set nopaste
```

---

### 6.用户和用户组管理

#### (1)groupadd命令

创建用户组

语法：`groupadd [OPTION]... GROUP`

- `[OPTION]...`   选项（可连续使用多个选项）
- `GROUP`   组名

| OPTION   | 作用       | 示例                    | 结果                           |
| -------- | ---------- | ----------------------- | ------------------------------ |
| `-g GID` | 指定组ID   | `groupadd -g 12345 pai` | 创建一个名为pai且ID为12345的组 |
| `-r`     | 创建系统组 | `groupadd -r app`       | 创建一个名为app的系统组        |

#### (2)groupdel命令

删除用户组

语法：`groupdel [OPTION]... GROUP`

- `[OPTION]...`   选项（可连续使用多个选项）
- `GROUP`   组名

> [!IMPORTANT]
>
> `groupdel`的限制**：**不能删除仍有成员的主组，否则报错 `cannot remove the primary group of user 'xxx'`。必须先移除用户或改变用户的主组
>
> 删除组前先 `grep username /etc/group` 确认该组是否还有用户引用

> `/etc/group` 文件的格式：`组名:密码:GID:成员列表`（密码字段通常为 `x` 或空，成员列表以逗号分隔）

#### (3)useradd命令

创建用户

语法：`useradd [OPTION]... LOGIN`

- `[OPTION]...`   选项（可连续使用多个选项）
- `LOGIN`   用户名

| OPTION      | 作用                                                 | 示例                          | 结果                                                         |
| :---------- | :--------------------------------------------------- | ----------------------------- | ------------------------------------------------------------ |
| `-m`        | 自动创建家目录（CentOS 7 默认自动创建，无需加 `-m`） | `useradd -m pai`              | 创建了一个名为pai的用户，同时创建家目录                      |
| `-M`        | 不创建家目录                                         | `useradd -M pai`              | 创建了一个名为pai的用户，但不创建家目录                      |
| `-N`        | 不创建同名组                                         | `useradd -N pai`              | 创建了一个名为pai的用户，不创建同名组，主组设为系统默认组（通常是 `users`） |
| `-d 目录`   | 指定家目录路径（配合 `-m` 生效）                     | `useradd -m -d /home/hai pai` | 创建了一个名为pai的用户，且家目录为 /home/hai                |
| `-s shell`  | 指定登录Shell（如 `/bin/bash`）                      | `useradd -s /bin/bash pai`    | 创建了一个名为pai的用户，且使用bash登录                      |
| `-g 主组`   | 指定用户的主组（组名或GID）                          | `useradd -g hai pai`          | 创建了一个名为pai的用户，该用户的主组为hai                   |
| `-G 附加组` | 指定附加组（可多个，逗号分隔）                       | `useradd -G hai,mai pai`      | 创建了一个名为pai的用户，该用户的主组为pai，附加组为hai、mai |
| `-u UID`    | 指定用户ID                                           | `useradd -u 325433 pai`       | 创建了一个名为pai的用户，该用户的UID为325433                 |
| `-r`        | 创建系统用户（UID<1000）                             | `useradd -r app`              | 创建了一个名为app的系统用户                                  |
| `-e 日期`   | 设置账号过期日期（格式 YYYY-MM-DD）                  | `useradd -e 2026-12-12 pai`   | 创建了一个名为pai的用户，且pai用户会在2026年12月12日被锁定   |
| `-c 注释`   | 添加用户描述（如全名、用途）                         | `useradd -c "派大星" pai`     | 创建了一个名为pai的用户，并注释全名为派大星，（查看 `/etc/passwd` 文件可以找到注释） |

> 家目录模板来自 `/etc/skel`，新用户的 `.bashrc` 等配置文件会从这里复制

#### (4)userdel命令

删除用户

语法：`userdel [OPTION]... LOGIN`

- `[OPTION]...`   选项（可连续使用多个选项）
- `LOGIN`   用户名

| OPTION | 作用                               | 示例             | 结果                                     |
| ------ | ---------------------------------- | ---------------- | ---------------------------------------- |
| `-r`   | 删除用户的同时删除其家目录和邮件池 | `userdel -r pai` | 删除pai用户的同时删除pai的家目录和邮件池 |

> [!WARNING]
>
> 默认（不加 `-r`）不删除家目录，用户残留数据会有安全风险

> 邮件池路径： `/var/spool/mail/<username>`

#### (5)usermod命令

修改用户属性

语法：`usermod [OPTION]... LOGIN`

- `[OPTION]...`   选项（可连续使用多个选项）
- `LOGIN`   用户名

| OPTION        | 作用                                 | 示例                           | 结果                                                      |
| ------------- | ------------------------------------ | ------------------------------ | --------------------------------------------------------- |
| `-g 主组`     | 修改主组（GID也行）                  | `usermod -g hai pai`           | pai用户的主组被修改为hai组                                |
| `-G 附加组`   | 覆盖当前附加组列表，会清空原有附加组 | `usermod -G hai,mai pai`       | pai用户的附加组被替换为hai组、mai组                       |
| `-aG 附加组`  | 追加当前附加组列表，会保留原有附加组 | `usermod -aG hai,mai pai`      | pai用户的附加组中被追加hai组和mai组，不影响其原有的附加组 |
| `-l 新用户名` | 修改用户名                           | `usermod -l hai pai`           | pai用户的用户名被修改为hai                                |
| `-s shell`    | 修改登录shell                        | `usermod -s /sbin/nologin pai` | pai用户的登录shell被设为/sbin/nologin（禁止登录）         |

> [!WARNING]
>
> `usermod -G docker alice` 会把 `alice` 原有的 `wheel` 等附加组**全部清空**，导致权限丢失，必须用 `-aG` 追加
>
> 修改组后，当前会话不会自动刷新组信息，需重新登录或执行 `newgrp`

##### 案例

1. 修改用户名后，家目录不会自动改名

如果你希望同步修改家目录，需要手动操作：

```shell
usermod -l hai pai
usermod -d /home/hai -m hai
```

其中 `-m` 会把旧家目录的内容移动到新位置

#### (6)groupmod命令

修改组信息

语法：

`groupmod [OPTION]... GROUP`

- `[OPTION]...`   选项（可连续使用多个选项）
- `LOGIN`   组名

| OPTION | 作用      | 示例                     | 结果                     |
| ------ | --------- | ------------------------ | ------------------------ |
| `-n`   | 修改组名  | `groupmod -n hai pai`    | 将pai组改名未hai组       |
| `-g`   | 修改组GID | `groupmod -g 654321 pai` | 将pai组的GID修改为654321 |

> 修改组GID后，应当及时修改对应用户的`/etc/passwd`，即使用`usermod -g`命令，**同时必须找出所有属于该组的文件提前修改**
>
> 组名修改不用管，其它地方会自己修改，其它地方只认GID，组名只是给人看的，不影响环境

#### (7)gpasswd命令

管理组成员和组管理员

语法：

`gpasswd [OPTION]... GROUP`

- `[OPTION]...`   选项（可连续使用多个选项）
- `GROUP`   组名

| OPTION | 作用           | 示例                     | 结果                                                       |
| ------ | -------------- | ------------------------ | ---------------------------------------------------------- |
| `-a`   | 添加用户到组   | `gpasswd -a pai hai`     | 将pai用户添加至hai组                                       |
| `-d`   | 从组中移除用户 | `gpasswd -d pai hai`     | 将pai用户从hai组移除                                       |
| `-M`   | 批量设置组成员 | `gpasswd -M pai,hai mai` | 将pai用户和hai用户添加至mai组（mai组原有成员会被全部清空） |

#### (8)passwd命令

修改用户密码

语法：`passwd [OPTION]... <accountName>`

- `[OPTION]...`   选项（可连续使用多个选项）
- `accountNAME`   用户名

| OPTION    | 作用                                 | 示例            | 结果                                                         |
| --------- | ------------------------------------ | --------------- | ------------------------------------------------------------ |
| `-l`      | 锁定用户                             | `passwd -l`     | 因为没有指定用户名，所以锁定了当前用户，**root用户绝对禁止在没有指定用户名的时候进行操作，指定root用户也绝对不行！！！**（不影响当前会话，下次登录时才无法登录） |
| `-u`      | 解锁用户                             | `passwd -u pai` | 只有root用户才能操作，如果root用户被锁了只能使用VNC等救援模式重置 |
| `-S`      | 查看指定用户的密码状态               | `passwd -S`     | 因为没有指定用户名，所以查看了当前用户的密码状态             |
| `-e`      | 强制下次登录修改密码                 | `passwd -e`     | 因为没有指定用户名，所以当前用户下次登录的时候会被强制要求修改密码 |
| `-x 天数` | 设置密码的最大有效期，到期后需要修改 | `passwd -x 10`  | 因为没有指定用户名，所以当前用户10天后再次登录的时候会被强制要求修改密码 |

```shell
echo "NewPass123" | passwd --stdin pai
```

可以在脚本里完成改密，不需要人工干预

> [!CAUTION]
>
> **再次提醒，绝对不允许锁root用户**

> 普通用户只能使用该命令操作自己的用户，只有root用户才能操作所有用户

#### (9)chage命令

设置密码有效期

`chage [OPTION]... LOGIN`

- `[OPTION]...`   选项（可连续使用多个选项）
- `LOGIN`   用户名

| OPTION | 作用                               | 示例                      | 结果                                                         |
| ------ | ---------------------------------- | ------------------------- | ------------------------------------------------------------ |
| `-M n` | 设置密码最大有效期                 | `chage -M 90`             | 因为没有指定用户名，所以当前用户的密码被设置为90天最大有效期，90天后需要重新设置密码 |
| `-m n` | 设置最小修改间隔                   | `chage -m 7`              | 因为没有指定用户名，所以当前用户的密码被设置为每次修改密码的间隔至少需要大于等于7天（如果上一次修改密码和这次修改密码的间隔小于7天则会被拒绝，**root用户除外，root用户想怎么改就怎么改**） |
| `-W n` | 到期前n天警告                      | `chage -W 14`             | 因为没有指定用户名，所以当前用户的密码还有14天到期的时候会被警告 |
| `-l`   | 查看当前策略                       | `chage -l`                | 因为没有指定用户名，所以会查看当前用户的策略                 |
| `-I n` | 过期后多长时间内未修改密码自动锁定 | `chage -M 90 -I 5`        | 因为没有指定用户名，所以当前用户的密码被设置为最大90天有效期，90天后需要重新设置密码，如果密码到期后5天内没有重新设置密码将会锁定用户（**绝对不要把root用户锁了**） |
| `-E`   | 设置账号过期日期                   | `chage -E 2026-12-12 pai` | 设置后，pai用户在该日期之后无法登录，但不会删除用户，也不会影响其已有文件 |

> [!CAUTION]
>
> **这里也绝对不允许锁root用户，敢锁root给你🐎杀了**

> `-I` 必须配合 `-M` 使用，单独设置 `-I` 无实际效果

#### (10)id命令

查询用户信息

语法：` id [OPTION]... [USER]`

- `[OPTION]...`   选项（可连续使用多个选项）
- `[USER]`   用户名

| OPTION | 作用                                     | 示例     | 结果                                                         |
| ------ | ---------------------------------------- | -------- | ------------------------------------------------------------ |
| `-u`   | 只显示UID                                | `id -u`  | 没有指定用户名，所以默认显示当前用户的UID                    |
| `-g`   | 只显示主组                               | `id -g`  | 没有指定用户名，所以默认显示当前用户的主组的GID              |
| `-G`   | 显示所有组                               | `id -G`  | 没有指定用户名，所以默认显示当前用户的所有组的GID            |
| `-n`   | 显示名字而不是数字（需要配合前三者使用） | `id -un` | 没有指定用户名，所以默认显示当前用户的名称（非UID，其它两个选项同理） |

> `cat /etc/group`即可查看所有组信息，`cat /etc/passwd` 即可查看所有用户信息

#### (11)getent命令

查询用户信息

`getent [OPTION]... database [key]...`

- `[OPTION]...`   选项（可连续使用多个选项）
- `database`   数据库类型
- `[key]...`   指定要查询的条目（key1 key2 ，使用空格隔开）

| OPTION       | 作用            | 示例                   | 来源                          |
| ------------ | --------------- | ---------------------- | ----------------------------- |
| `-s service` | 指定NSS服务来源 | `getent -s files`      | 限制只查询来源于本地的信息    |
| `-i`         | 忽略大小写      | `getent -i passwd Pai` | 即便是名为pai的用户也会被查到 |

| NSS服务来源 | 数据来源                                               |
| :---------- | :----------------------------------------------------- |
| `files`     | 本地文件（`/etc/passwd`、`/etc/group`、`/etc/shadow`） |
| `ldap`      | LDAP 目录服务器（如 OpenLDAP、Active Directory）       |
| `nis`       | NIS（网络信息服务），老式集中用户管理                  |
| sss         | SSSD（系统安全服务守护进程），连接 AD、LDAP 等         |

#### (12)groups命令

查看哪些用户属于哪些组，比 `id` 更直观

语法：

`groups [OPTION]... [USER]...`

- `[OPTION]...`   选项（可连续使用多个选项）
- `[USER]`   用户名（可以连续使用多个）

> groups确实有选项，但是意义不大

#### (14)chown命令

修改文件所有者（属主）和所属组

语法：

`chown [OPTION]... [OWNER][:[GROUP]] FILE...`

- `[OPTION]...`   选项（可连续使用多个）
- `[OWNER]`   新的所有者（用户名或 UID）
- `[GROUP]`   新的所属组（组名或 GID），可省略；若写 `:` 但不指定组，则组改为所有者的主组
- `FILE...`   目标文件或目录（可多个）

| OPTION              | 作用                                               | 示例                                   | 结果                                                         |
| :------------------ | :------------------------------------------------- | :------------------------------------- | :----------------------------------------------------------- |
| `-R`                | 递归修改目录及其所有子文件/子目录                  | `chown -R pai:devteam /srv/app`        | 将 /srv/app 下所有内容的所有者改为 pai，组改为 devteam       |
| `-v`                | 显示每个文件的修改过程                             | `chown -v pai file.txt`                | 输出 `changed ownership of 'file.txt' from root to pai`      |
| `-c`                | 仅在发生改变时显示信息                             | `chown -c pai file.txt`                | 只有所有者真的变了才输出                                     |
| `--reference=RFILE` | 按参考文件（目录）的所有者和组设置目标文件（目录） | `chown --reference=ref.txt target.txt` | target.txt 的所有者和组与 ref.txt 相同                       |
| `-h`                | 修改符号链接本身，而非链接指向的目标               | `chown -h pai symlink`                 | 只改符号链接的属主，不影响目标文件（`chown` 默认不跟随符号链接，直接对符号链接操作会失败；加 `-h` 才修改链接本身） |

> [!IMPORTANT]
>
> `-R --reference` 会把目录和文件一视同仁，改之前搞清楚你到底准备做什么，如果为了偷懒给权限配错了给你🐎杀了

> 只有 root 能修改文件（含符号链接）的所有者；普通用户仅能修改自己文件的组为自己所属的组，不能用 chown 将文件所有者改为他人（即使文件是自己的）

#### (13)useradd与usermod的-s选项取值

| -s 取值              | 效果                   | 使用场景          |
| :------------------- | :--------------------- | :---------------- |
| `/bin/bash`          | 允许登录，使用 bash    | 普通用户          |
| `/bin/sh`            | 允许登录，使用 sh      | 脚本兼容场景      |
| `/sbin/nologin`      | 禁止登录，但可运行服务 | 系统用户/服务账号 |
| `/bin/false`         | 禁止登录，立即退出     | 更严格的禁登录    |
| `/usr/bin/git-shell` | 只允许 git 操作        | Git 专用账号      |

---

### 7.权限控制

#### (1)su命令

切换至指定用户

语法：

`su [OPTION]... [-] [<USER> [<argument>...]]`

- `[OPTION]...`   选项（可连续使用多个选项）
- `<USER>`   用户名
- `<argument>...`   参数

| OPTION | 作用                                               | 示例                             | 结果                                                         |
| ------ | -------------------------------------------------- | -------------------------------- | ------------------------------------------------------------ |
| `-`    | 模拟登录shell的方式登录用户                        | `su - pai`                       | 登录pai用户                                                  |
| `-c`   | 临时切换至指定用户执行命令，执行后退出             | `su -c "echo 'hello world'" pai` | 临时切换至pai用户执行命令，执行后退回原用户（需要执行的命令建议全部使用 “” 包围） |
| `-s`   | 登录时指定目标用户的shell，覆盖其默认shell         | `su -s /bin/bash pai`            | 登录pai用户的时候指定登录shell为/bin/bash                    |
| `-m`   | 保留当前用户的环境变量，不重置为目标用户的登录环境 | `su -m pai`                      | 切换至pai用户但是不改变当前的环境变量及位置                  |

| 行为                         | `su -`         | `su -m`      |
| :--------------------------- | :------------- | :----------- |
| 工作目录                     | 切到目标家目录 | 保持当前目录 |
| 执行登录初始化文件           | ✔              | ❌            |
| 重置 `USER`、`HOME`、`SHELL` | ✔              | ❌            |
| 重置 `PATH` 等环境变量       | ✔              | ❌            |
| 加载 `.bash_profile` 等      | ✔              | ❌            |

> [!NOTE]
>
> 在生产环境中应当极力避免使用`su`而是使用`su -`切换用户，因为不带 `-` 的 `su` 保留了大量原用户的环境变量，这些变量可能包含 **危险的自定义PATH**或**恶意函数**，进入高权限环境后可能导致意外执行错误命令（敢用给你🐎杀了）。

> `-s` 指定的 shell 必须是 `/etc/shells` 中存在的，否则可能被 PAM[^1] 拒绝
>
> `su` 认证的是目标用户的密码（普通用户切 root 需输入 root 密码，root 切任何用户免密）；而 `sudo` 认证的是当前用户的密码

#### (2)sudo命令

临时以root用户的身份执行命令

语法：

`sudo [OPTION]... argument...`

- `[OPTION]...`   选项（可连续使用多个选项）
- `argument...`   参数（你具体要执行的命令）

| OPTION         | 作用                                                   | 示例                      | 结果                                                         |
| -------------- | ------------------------------------------------------ | ------------------------- | ------------------------------------------------------------ |
| （不指定选项） | 以root身份执行命令，执行完立即回到原用户身份           | `sudo ls -la`             | 以root用户的权限执行了`ls -la`命令                           |
| `-u`           | 以指定用户身份执行命令（默认 root）                    | `sudo -u hai ls -la`      | pai用户以hai用户的身份执行`ls -la`命令                       |
| `-i`           | 模拟 root 登录                                         | `sudo -i`                 | 切换至root用户                                               |
| `-s`           | 以 root 身份启动一个 shell（保留当前环境，不模拟登录） | `sudo -s`                 | 以 root 身份启动 shell，保留当前工作目录和环境变量，但会加载root用户的 `.bashrc`（与 `su -m` 不同） |
| `-l`           | 列出当前用户可执行的 sudo 命令                         | `sudo -l`                 | 显示当前用户的sudo权限列表（含是否需要密码）                 |
| `-v`           | 验证 sudo 权限，延长缓存时间                           | `sudo -v`                 | 实际上在5分钟内无需输入密码就可以直接运行`sudo`（可在 `/etc/sudoers` 中修改 `timestamp_timeout` 配置，还得去PAM[^1]里面改） |
| `-k`           | 使 sudo 缓存失效，下次需要重新输密码                   | `sudo -k`                 | 下一次执行`sudo`的时候就需要输入密码                         |
| `-n`           | 非交互模式，不提示输入密码                             | `sudo -n`                 | 若当前有有效的认证时间戳，则执行命令；若没有（或已过期），则直接报错退出，不会提示输入密码 |
| `-g`           | 以指定组身份执行命令                                   | `sudo -g devteam ls /srv` | 以devteam组身份执行 `ls /srv`                                |

> [!IMPORTANT]
>
> 执行单条特权命令时用 `sudo`；需要交互式 shell 时用 `sudo -i`（有审计日志）。`sudo` 的所有操作都记录在 `/var/log/secure`（CentOS 7）中，便于审计

> `/etc/sudoers` 默认配置是15分钟，但 CentOS 7 系统默认启用了 PAM 模块的 `pam_timestamp.so`，它的默认值就是5分钟，所以在CentOS系统中，缓存实际生效时间应当为5分钟
>
> `sudo` 默认验证当前用户的密码，且 CentOS 7 需要当前用户在 `wheel` 组（或 `/etc/sudoers` 中明确授权）才能执行

**sudo权限的配置方法（唯一安全的方式，不用这个配给你🐎杀了）**

在 `/etc/sudoers` 里（用 `visudo` 编辑），可以这样配置：

```shell
# 让 wheel 组用户执行所有命令都不需要密码（风险高，不建议）
%wheel ALL=(ALL) NOPASSWD: ALL

# 让某个用户执行特定命令不需要密码（推荐，最小授权）
pai ALL=(ALL) NOPASSWD: /usr/bin/systemctl restart nginx, /usr/bin/systemctl stop nginx 
#/usr/bin/systemctl COMMAND...,/usr/bin/systemctl COMMAND...(使用前可以用which systemctl查一下是不是/usr/bin/systemctl)
```

##### 案例

1. 如何在脚本中使用sudo

```shell
#!/bin/bash
# 1. 延长 sudo 缓存（首次会提示输入密码）
sudo -v

# 2. 使用 sudo -n 执行命令（不会卡住等待密码）
sudo -n systemctl restart nginx

# 3. 执行完后清除缓存，避免权限泄露
sudo -k
```

#### (3)who am i/whoami命令

##### ①who am i命令

查询**原始登录**的用户的信息

语法：

`who am i`

示例：

```shell
pai         pts/1        2026-04-25 23:57 (49.95.193.74)
当前登录的用户 当前终端          登录时间           来源IP
```

> 如果当前用户是通过本地终端（tty）登录而不是 SSH，则 `来源IP` 字段会显示为空或 `localhost`

##### ②whoami命令

查询**当前登录**的用户的信息

语法：

`whoami`

#### (4)exit命令

退出当前shell进程

语法：

`exit`

| 场景                            | `exit` 的效果                |
| :------------------------------ | :--------------------------- |
| 普通用户登录的 shell            | 退出登录，回到登录界面       |
| `su - root` 切换后的 root shell | 退回普通用户                 |
| `sudo -i` 切换后的 root shell   | 退回普通用户                 |
| 脚本中的 subshell               | 结束该子 shell，返回父 shell |

> [!NOTE]
>
> 切记在每次使用完成后一定要退回普通用户，否则shell会层层堆叠

#### (5)uname命令

查询当前的操作系统

语法：

`uname [OPTION]`

- `[OPTION]...`   选项（可连续使用多个选项）

| OPTION         | 作用             | 示例       | 结果                                                        |
| :------------- | :--------------- | ---------- | :---------------------------------------------------------- |
| （不指定选项） | 显示操作系统名称 | `uname`    | `Linux`                                                     |
| `-a`           | 显示所有信息     | `uname -a` | `Linux server1 3.10.0-1160.el7.x86_64 #1 SMP ... GNU/Linux` |
| `-r`           | 只显示内核版本   | `uname -r` | `3.10.0-1160.el7.x86_64`                                    |
| `-m`           | 显示硬件架构     | `uname -m` | `x86_64`                                                    |
| `-n`           | 显示主机名       | `uname -n` | `server1`                                                   |
| `-s`           | 显示内核名称     | `uname -s` | `Linux`                                                     |
| `-v`           | 显示内核编译时间 | `uname -v` | `#1 SMP Mon Oct 19 16:18:59 UTC 2020`                       |

> `#1`   内核编译次数（第一次编译）
>
> `SMP`    表示支持对称多处理

#### (6)chmod命令

修改权限信息

语法：`chmod [OPTION]... FILE...`

- `[OPTION]...`   选项（可连续使用多个选项）
- `FILE...`   目录或文件名称（可连续指定多个）

| OPTION                   | 作用                                           | 示例                             | 结果                                                 |
| ------------------------ | ---------------------------------------------- | -------------------------------- | ---------------------------------------------------- |
| `-R` | 递归修改目录及其所有内容                       | `chmod -R 755 /data`             | 将 `/data` 目录及其下所有文件和子目录权限设为 755    |
| `-v`   | 详细显示每个文件权限的变更过程                 | `chmod -v 644 file.txt`          | 显示 `mode of 'file.txt' changed to 0644` 等详细信息 |
| `-c`   | 仅在文件权限确实发生改变时，才显示详细信息 | `chmod -c 644 file.txt`          | 如果权限原本就是 644，则不输出；如果变了才输出       |
| `-f`          | 静默模式，尽可能不报错（命令本身的语法错误仍会报错） |chmod -f 777 none.txt|即使文件不存在，也不提示任何错误|
| `--reference=RFILE` | 按参考文件（目录）的权限设置目标文件（目录） |`chmod --reference=ref.txt target.txt`|target.txt 的权限与 ref.txt 完全相同（包括特殊权限位）|
| `--preserve-root`    | 禁止对根目录 `/` 递归操作，是 `-R` 的安全防护  | `chmod -R --preserve-root 777 /` | 它只保护 `/` 本身不被修改，子目录仍会被递归修改，所以这不是安全命令，只是防🐖 |

> [!IMPORTANT]
>
> `-R --reference` 会把目录和文件一视同仁，改之前搞清楚你到底准备做什么，如果为了偷懒给权限配错了给你🐎杀了

**第一部分：指定用户（Who）**

| 字母 | 含义     | 解释               |
| :--- | :------- | :----------------- |
| `u`  | 所有者   | User/Owner         |
| `g`  | 所属组   | Group              |
| `o`  | 其他用户 | Others             |
| `a`  | 所有用户 | All (等价于 `ugo`) |

**第二部分：操作符（Operator）**

| 符号 | 含义                                         |
| :--- | :------------------------------------------- |
| `+`  | 添加权限                                     |
| `-`  | 移除权限                                     |
| `=`  | 精确设置权限（会覆盖该类别用户的所有旧权限） |

**第三部分：权限位（Permissions）**

| 字母 | 含义     | 解释                                                   |
| :--- | :------- | :----------------------------------------------------- |
| `r`  | 读       | Read                                                   |
| `w`  | 写       | Write                                                  |
| `x`  | 执行     | eXecute                                                |
| `X`  | 智能执行 | 仅当目标是**目录**或**已有执行权限的文件**时才添加 `x` |
| `s`  | 特殊权限 | setuid (4) 或 setgid (2)                               |
| `t`  | 粘滞位   | sticky，常用于 `/tmp` 防止用户互删文件                 |

**数字与权限位对照表**：

| 数值  | 权限  | 说明       |
| :---- | :---- | :--------- |
| **0** | `---` | 无权限     |
| **4** | `r--` | 读         |
| **5** | `r-x` | 读+执行    |
| **6** | `rw-` | 读+写      |
| **7** | `rwx` | 读+写+执行 |

**特殊权限位:**

| 特殊权限 | 符号  | 数字 | 用在谁身上      | 作用                                                         |
| :------- | :---- | :--- | :-------------- | :----------------------------------------------------------- |
| setuid   | `u+s` | 4    | 可执行文件      | 任何用户执行的时候都以文件**属主**身份运行（root用户不受限制） |
| setgid   | `g+s` | 2    | 可执行文件/目录 | 任何用户执行的时候都以文件**属组**身份运行；目录内新文件继承目录组（root用户不受限制） |
| sticky   | `+t`  | 1    | 目录            | 只有文件属主（root想改哪就改哪）能删除自己的文件             |

> [!NOTE]
>
> Linux 中符号链接的权限位无法被修改（始终为 777），`chmod` 默认**跟随符号链接**，修改的是链接指向的目标文件。若想操作链接本身，需用 `-h` 选项（但会报错或无效）（你敢偷懒不找原文件而是直接修改符号链接我给你🐎杀了）
>
> 在递归修改目录权限时，`chmod -R` 会连同目录内所有文件一起修改，使用前务必确认范围，操作不当可能导致服务因权限问题而无法访问
>
> `chmod` 符号模式（`+`、`-`、`=`）在省略 `who` 时，默认对 `a` 操作，但会受当前 `umask` 影响，被屏蔽的权限位不会生效（你敢偷懒忽略`who`我给你🐎杀了）

> [!WARNING]
>
> **杀🐎警告：**
>
> setuid 和 setgid 是高风险权限，如果给不安全的程序设置，攻击者可能利用它提权。生产环境应谨慎使用，尽量用 `sudo` 替代 setuid 程序

#### (7)umask命令

创建新文件或目录时的默认权限设置

语法：

`umask [OPTION]... [MODE]`

- `[OPTION]...`   选项（可连续使用多个）
- `[MODE]`：要设置的权限掩码，八进制（如 `022`）或符号形式（如 `u=rwx,g=rx,o=`）(省略时显示当前 umask)

| OPTION     | 作用                                       | 示例        | 结果                                |
| :--------- | :----------------------------------------- | :---------- | :---------------------------------- |
| （无参数） | 显示当前 umask（八进制）                   | `umask`     | `0022`                              |
| `-S`       | 以符号形式显示当前 umask                   | `umask -S`  | `u=rwx,g=rx,o=rx`                   |
| `-p`       | 输出 `umask 数值` 形式，可用于脚本保存恢复 | `umask -p`  | `umask 022`                         |
| `MODE`     | 设置 umask，八进制（0-7）或符号形式        | `umask 027` | 立即生效，只影响当前 shell 及子进程 |

> [!NOTE]
> 符号形式设置时，指定的权限是**要保留的权限**，而不是掩码。例如 `umask u=rwx,g=rx,o=` 等价于数字 `027`（其他用户无任何权限）
>
> 每个进程都有独立的 umask，继承自父进程，但修改后不影响父进程或已运行的其他进程。

> **永久设置 umask 的位置**
>
> 全局：`/etc/profile`、`/etc/bashrc`（影响所有用户）
>
> 用户级：`~/.bashrc`、`~/.bash_profile`
>
> 服务：systemd unit 文件 `UMask=` 参数，或 init 脚本中显式设置

---

### 8.软件安装

#### (1)yum命令

从仓库中安装软件，全自动安装，无需考虑依赖等问题

语法：

`yum [OPTION]... [COMMAND] [PACKAGE]...`

- `[OPTION]...`：选项（可连续使用多个）
- `[COMMAND]`：操作命令（install/remove/update/search 等）
- `[PACKAGE]...`：软件包名（可多个）

| OPTION               | 作用         | 备注                           |
| :------------------- | :----------- | :----------------------------- |
| `-y`                 | 自动确认     | 脚本里必须加，否则会卡住等输入 |
| `-q`                 | 安静模式     | 减少输出，脚本里常用           |
| `--downloadonly`     | 只下载不安装 | 下载到缓存目录                 |
| `--downloaddir=路径` | 指定下载目录 | 配合 `--downloadonly`          |
| `--enablerepo`       | 启用指定仓库 | 临时启用某个源                 |
| `--disablerepo`      | 禁用指定仓库 | 临时禁用某个源                 |

| COMMAND                 | 作用               | 示例                                              | 结果                                                        |
| :---------------------- | :----------------- | :------------------------------------------------ | :---------------------------------------------------------- |
| `yum install`           | 安装软件包         | `yum install nginx -y`                            | 自动解决依赖，`-y` 跳过确认                                 |
| `yum groups install`    | 安装软件组         | `yum groups install "Development Tools"`          | 自动下载Development Tools软件组                             |
| `yum remove`            | 卸载软件包         | `yum remove nginx -y`                             | 会同时卸载依赖它的包                                        |
| `yum update`            | 更新所有包         | `yum update -y`                                   | **生产环境慎用**，可能更新内核导致重启                      |
| `yum update 包名`       | 更新指定包         | `yum update nginx`                                | 只更新 nginx                                                |
| `yum search`            | 搜索软件包         | `yum search nginx`                                | 按名称/描述搜索                                             |
| `yum info`              | 查看软件包信息     | `yum info nginx`                                  | 显示版本、大小、简介                                        |
| `yum list`              | 列出已安装/可用包  | `yum list installed`                              | 列出所有已安装                                              |
| `yum provides`          | 查某命令属于哪个包 | `yum provides /usr/bin/tree`                      | **超实用**，找不到命令时用（可以使用which命令查找命令路径） |
| `yum clean all`         | 清理缓存           | `yum clean all`                                   | 缓存损坏或删除缓存时使用（镜像中最好把缓存删了）            |
| `yum history`           | 查看安装/卸载历史  | `yum history`                                     | 查看安装/卸载历史                                           |
| `yum history undo 编号` | 回滚指定操作       | `yum history undo 12387`                          | 回滚指定操作                                                |
| `yum localinstall`      | 自动寻找缺失的依赖 | `yum localinstall nginx-1.20.1-10.el7.x86_64.rpm` | 安装本地rpm文件，并自动从仓库解决依赖                       |

> yum 从仓库下载RPM包，仓库配置在 `/etc/yum.repos.d/.repo`，每个仓库定义了：仓库 URL（http/ftp/file）、是否启用（`enabled=1`）、是否检查签名（`gpgcheck=1`）
>
> yum 的本质实际上时 RPM 包的前端管理器，yum 负责找到它们、解决依赖、按顺序安装，所以 yum 可以同时下载指定软件的各种依赖，无需手动配置

**yum换源**

因为CentOS7以及之前的版本均已停止维护，所以其官方源也通常无法使用，因此我们需要更换yum源来保证当前系统可以正常使用yum命令

```shell
# 备份原配置，防止配置错误导致换源失败（直接拍快照也行）
cd /etc/yum.repos.d
cp CentOS-Base.repo CentOS-Base.repo.bak
# 换成阿里云的yum源
curl -o /etc/yum.repos.d/CentOS-Base.repo http://mirrors.aliyun.com/repo/CentOS-7.repo
# 清除旧的yum缓存
yum clean all
# 生成新的yum缓存（可选，在配置镜像这种需要尽可能减少大小的时候可以选择不生成yum缓存）
yum makecache
```

**EPEL换源**

CentOS7停服后，装 `epel-release` 后默认指向的 EPEL 源也可能慢或失效。换完yum源后，应当顺手给EPEL源也换了

```shell
# 下载epel-release
yum install epel-release -y
# 换成阿里云的EPEL源
curl -o /etc/yum.repos.d/epel.repo http://mirrors.aliyun.com/repo/epel-7.repo
# 清除旧的yum缓存
yum clean all
# 生成新的yum缓存（可选，在配置镜像这种需要尽可能减少大小的时候可以选择不生成yum缓存）
yum makecache
```

> [!WARNING]
>
> 生产环境中绝对不要全量更新（yum update -y），如果全量更新导致服务不兼容甚至系统崩溃我给你🐎杀了

#### 案例

1. yum 卡住不动了

	**现象**：`yum install` 一直没反应

	**排查**：

	```shell
	# 看是否有别的 yum 进程在跑
	ps aux | grep yum
	```

	**解决**：如果有残留进程，杀掉后清缓存：

	```shell
	rm -rf /var/run/yum.pid
	yum clean all
	```

#### (2)rpm命令

操作本地RPM包文件，手动安装，需自行解决依赖

语法：

`rpm [OPTION]... [PACKAGE]...`

- `[OPTION]...`：选项（可连续使用多个）
- `[PACKAGE]...`：软件包名或文件名

| OPTION        | 作用                 | 示例                                      | 结果                                                    |
| :------------ | :------------------- | :---------------------------------------- | :------------------------------------------------------ |
| `-qa`         | 列出所有已安装包     | `rpm -qa | grep nginx`                    | 查询 nginx 是否安装                                     |
| `-q 包名`     | 查指定包是否安装     | `rpm -q nginx`                            | 输出版本号或未安装                                      |
| `-ql 包名`    | 列出包安装的所有文件 | `rpm -ql nginx`                           | 看 nginx 装在哪                                         |
| `-qc 包名`    | 只列配置文件         | `rpm -qc nginx`                           | 看 nginx 的配置在哪                                     |
| `-qf 文件名`  | 查文件属于哪个包     | `rpm -qf /etc/nginx/nginx.conf`           | 反向查询                                                |
| `-qi 包名`    | 查看包的详细信息     | `rpm -qi nginx`                           | 版本、大小、安装时间                                    |
| `-ivh 文件名` | 安装 rpm 文件        | `rpm -ivh nginx-1.20.1-10.el7.x86_64.rpm` | 安装 nginx ，但需要手动解决依赖                         |
| `-Uvh 文件名` | 升级或安装           | `rpm -Uvh nginx-1.20.1-10.el7.x86_64.rpm` | 如果 nginx 已存在则升级                                 |
| `-e 包名`     | 卸载                 | `rpm -e nginx`                            | 删除了 nginx ，但不同时删除依赖（这里直接使用包名即可） |

> 一般情况下没网才会使用rpm，否则首先推荐使用yum

> [!WARNING]
>
> 关于两个rpm的`OPTION `
>
> `--nodeps`   告诉rpm忽略依赖关系检查
>
> `--force`   强制 rpm 执行操作，即使遇到冲突或错误也继续
>
> **除非你很清楚你现在在做什么**，否则绝对不要碰这俩选项，如果因此出现诡异错误我给你🐎杀了

#### (3)编译安装

纯手动古法安装，需要考虑所有问题

```shell
# 经典三步
./configure [OPTION]...	# 检查环境，生成 Makefile
make					# 编译源码
make install			# 安装到系统目录
```

| OPTION          | 作用                       | 示例                         |
| :-------------- | :------------------------- | :--------------------------- |
| `--prefix=路径` | 指定安装目录               | `--prefix=/usr/local/nginx`  |
| `--with-xxx`    | 启用某功能                 | `--with-http_ssl_module`     |
| `--without-xxx` | 禁用某功能                 | `--without-http_gzip_module` |
| `--help`        | 查看该源码包支持的所有选项 | `./configure --help`         |

> 默认 `--prefix` 是 `/usr/local`，所以不指定的话，软件会装到 `/usr/local/bin`、`/usr/local/lib` 等。但为了干净管理，建议每个源码包指定独立目录，如 `/usr/local/nginx`，方便卸载
>
> 当然你无脑塞进`/opt`也没有问题，因为这块就是专门拿来装软件的

> [!TIP]
>
> 编译前先 `./configure --help` 查看该源码包支持的所有选项，再决定 `--prefix` 和 `--with/--without` 怎么配

**编译工具链**（提前下载，否则无法进行编译安装）

```shell
# 直接下载软件组
yum groups install "Development Tools"
# 手动下载工具链
yum install gcc make pcre-devel openssl-devel zlib-devel -y
```

> 不装 `-devel` 包，configure 直接报错

> [!TIP]
>
> 默认单线程make，想要加速可以：
>
> ```shell
> # 自动获取CPU核数从而并行编译，可以显著提高速度
> ./configure [OPTION]...	# 检查环境，生成 Makefile
> make -j$(nproc)			# 编译源码（替换此处,但有些老旧的源码包不支持并行编译，可能出现诡异错误）
> make install			# 安装到系统目录
> ```

> [!IMPORTANT]
>
> 一定要指定路径，一定要指定路径，一定要指定路径！！！

1. 装完不知道装哪了

	**现象**：编译安装完成，但不知道命令在哪

	**原因**：没加 `--prefix`，装到了默认的 `/usr/local`

	**排查**：

	```shell
	# 找可执行文件
	find /usr/local -name "nginx"
	# 或者
	which nginx
	```

	**预防**：编译时一定要加 `--prefix=/usr/local/软件名`

2. 卸载源码安装的软件

	**现象**：源码安装没有 `yum remove`，也不知道怎么卸载

	**解决**：

	如果指定了 `--prefix`，直接 `rm -rf /usr/local/软件名`

	如果没有指定，那麻烦了，文件散落在各处，只能手动找

	**预防**：**永远指定 `--prefix`，卸载就是删一个目录**

3. 编译安装后命令不在 PATH 里

	**现象：**编译装完 Nginx 后，直接敲 `nginx` 可能出现 `command not found`

	**原因：**命令被装到了 `/usr/local/nginx/sbin/nginx`中，而 PATH 里只有 `/usr/local/bin`、`/usr/local/sbin` 等

	**解决**：

	```shell
	# 方案1：软链到 PATH 已有目录
	ln -s /usr/local/nginx/sbin/nginx /usr/local/bin/nginx
	
	# 方案2：把目录加进 PATH（写入 ~/.bashrc）
	echo 'export PATH=/usr/local/nginx/sbin:$PATH' >> ~/.bashrc
	source ~/.bashrc
	```

---

### 9.systemd服务

#### (1)systemctl命令

服务管理

语法：`systemctl [OPTION]... COMMAND [UNIT]...`

- `[OPTION]...`   选项（可连续使用多个）
- `COMMAND`   操作命令（start/stop/status/enable 等）
- `[UNIT]...`   服务单元名，如 `nginx.service`、`sshd.service`，`.service` 可省略

| OPTION          | 作用                      | 示例                                     | 结果                                                     |
| :-------------- | :------------------------ | :--------------------------------------- | -------------------------------------------------------- |
| `--now`         | 同时执行 start/stop       | `systemctl enable --now nginx`           | 启用 nginx 开机自启并立即启动                            |
| `--no-pager`    | 不进入分页模式            | `systemctl status --no-pager nginx`      | 输出服务状态的时候不进入分页模式                         |
| `-l` / `--full` | 显示完整行，不截断        | `systemctl list-units --type=service -l` | 列出所有已加载的 unit时显示完整行                        |
| `--failed`      | 检查所有启动失败的服务    | `systemctl --failed`                     | 列出所有启动失败的服务                                   |
| `--user`        | 管理用户级服务（非 root） | `systemctl --user start podman`          | 普通用户可以使用该命令启动 podman 服务，避免污染系统服务 |

> `--now` 可以配合 enable/disable/mask/unmask 使用，立即执行对应的 start/stop，实际上可以理解为：现在就立即执行

| 命令                          | 作用                                     | 示例                                        | 结果/说明                                                    |
| :---------------------------- | :--------------------------------------- | :------------------------------------------ | :----------------------------------------------------------- |
| `systemctl start`             | 启动服务                                 | `systemctl start nginx`                     | 立即启动，不持久                                             |
| `systemctl stop`              | 停止服务                                 | `systemctl stop nginx`                      | 立即停止                                                     |
| `systemctl restart`           | 重启服务                                 | `systemctl restart nginx`                   | 先 stop 再 start                                             |
| `systemctl try-restart`       | 仅在服务运行时重启                       | `systemctl try-restart nginx`               | 只有服务已经在运行了才重启                                   |
| `systemctl reload`            | 重新加载配置                             | `systemctl reload nginx`                    | 不中断服务，只重读配置（有的服务不支持，如果报错了还是得重启） |
| `systemctl status`            | 查看服务状态                             | `systemctl status nginx`                    | 显示是否运行、PID、最近日志                                  |
| `systemctl show`              | 查看服务的所有底层属性（比 status 详细） | `systemctl show nginx | grep Memory`        | 查内存限制                                                   |
| `systemctl enable`            | 设置开机自启                             | `systemctl enable nginx`                    | 创建符号链接                                                 |
| `systemctl disable`           | 取消开机自启                             | `systemctl disable nginx`                   | 删除符号链接                                                 |
| `systemctl is-enabled`        | 查是否开机自启                           | `systemctl is-enabled nginx`                | 输出 enabled/disabled/static                                 |
| `systemctl is-active`         | 查是否正在运行                           | `systemctl is-active nginx`                 | 输出 active/inactive                                         |
| `systemctl cat`               | 直接查看 unit 文件内容                   | `systemctl cat nginx`                       | 显示 nginx.service 的完整内容                                |
| `systemctl edit`              | 只覆盖原 unit 配置中的部分参数           | `systemctl edit nginx`                      | 只需要写入你想修改的那几行，不需要写原文件的全部内容         |
| `systemctl daemon-reload`     | 重新加载 unit 文件                       | `systemctl daemon-reload`                   | **改完 unit 文件必须执行**                                   |
| `systemctl list-units`        | 列出所有已加载的 unit                    | `systemctl list-units --type=service`       | 只列服务（内存里正在运行/加载的，stop 之后就看不到了）       |
| `systemctl list-unit-files`   | 列出所有已安装的 unit                    | `systemctl list-unit-files --state=enabled` | 只看自启的（硬盘上静态安装的都能看到，stop 了也能看到）      |
| `systemctl list-dependencies` | 查看服务的依赖树                         | `systemctl list-dependencies nginx`         | 查看 nginx 服务的依赖树                                      |
| `systemctl kill`              | 杀掉服务的所有进程                       | `systemctl kill nginx`                      | 比 `stop` 更暴力                                             |
| `systemctl mask`              | 屏蔽服务，无法启动                       | `systemctl mask nginx`                      | 创建指向 `/dev/null` 的链接                                  |
| `systemctl unmask`            | 解除屏蔽                                 | `systemctl unmask nginx`                    | 恢复                                                         |

> `systemctl edit nginx`保存退出后，文件实际保存在 `/etc/systemd/system/nginx.service.d/override.conf`

> [!NOTE]
>
> `list-units` 可以配合 `--state` 过滤以查看运行状态
>
> ```
> systemctl list-units --state=failed     # 看失败的服务
> systemctl list-units --state=running    # 看正在运行的服务
> ```

> [!WARNING]
>
> 被 mask 后，不仅手动 `start` 无效，其他服务依赖它启动也会失效，如果 mask 某个服务**务必**告知其他人

#### (2)systemd unit文件

**存放位置**

| 路径                       | 优先级 | 用途                                     |
| :------------------------- | :----- | :--------------------------------------- |
| `/usr/lib/systemd/system/` | 低     | 软件包自带的 unit 文件                   |
| `/run/systemd/system/`     | 中     | 运行时临时 unit，重启消失                |
| `/etc/systemd/system/`     | **高** | 管理员自定义的 unit 文件，覆盖上面同名的 |

> [!CAUTION]
>
> 修改已存在的服务时，优先用 `systemctl edit` 做局部覆盖，不要直接改 `/usr/lib/systemd/system/` 下的原始文件，新建自己的 unit 文件时直接写 `/etc/systemd/system/` 即可
>

> 不要直接改 `/usr/lib/systemd/system/` 里面的，因为 yum 更新软件包时会覆盖那里的文件，你的修改会丢

**unit 文件结构**

```shell
[Unit]
Description=My Flask App              # 描述
After=network.target                  # 在网络就绪后才启动
Wants=nginx.service                   # 软依赖，nginx 挂了不影响本服务

[Service]
Type=simple                           # 进程类型
User=pai                              # 以哪个用户运行
Group=pai                             # 以哪个组运行
WorkingDirectory=/opt/myapp           # 工作目录
ExecStart=/usr/bin/python3 app.py     # 启动命令
ExecReload=/bin/kill -HUP $MAINPID    # reload 命令
Restart=on-failure                    # 失败时自动重启
RestartSec=5                          # 重启间隔 5 秒
Environment="FLASK_ENV=production"    # 环境变量
EnvironmentFile=/etc/myapp/env        # 环境变量建议用这个
LimitNOFILE=65535                     # 高并发服务必调
TimeoutStartSec=300					  # 启动超时时间 300 秒，超过则判定失败
TimeoutStopSec=30					  # 停止超时时间 30 秒，超过则强制杀

[Install]
WantedBy=multi-user.target            # 开机自启时挂到哪个 target
```

**[Unit] 段常用参数**

| 参数          | 作用                                 | 示例                       |
| :------------ | :----------------------------------- | :------------------------- |
| `Description` | 服务描述                             | `Description=My Flask App` |
| `After`       | 在指定服务/目标之后启动              | `After=network.target`     |
| `Before`      | 在指定服务/目标之前启动              | `Before=nginx.service`     |
| `Requires`    | 强依赖，依赖的服务挂了本服务也停     | `Requires=network.target`  |
| `Wants`       | 软依赖，依赖的服务挂了本服务不受影响 | `Wants=nginx.service`      |

> `Requires` 是“没有你我不行”，`Wants` 是“有你我更好”。生产环境里**优先用 `Wants`**，避免因为一个非核心依赖挂了导致你的服务也停掉

**[Service] 段关键参数**

| 参数               | 作用               | 备注                                             |
| :----------------- | :----------------- | :----------------------------------------------- |
| `Type`             | 进程启动类型       | 见下方表格                                       |
| `User`             | 运行用户           | **别用 root 跑服务（应当使用专门的系统用户跑）** |
| `Group`            | 运行组             | 同上                                             |
| `WorkingDirectory` | 工作目录           | 相对路径在这里生效                               |
| `ExecStart`        | 启动命令           | **必须用绝对路径**                               |
| `ExecStop`         | 停止命令           | 可选，默认杀进程                                 |
| `ExecReload`       | reload 命令        | 可选                                             |
| `Restart`          | 重启策略           | `on-failure` 最常用                              |
| `RestartSec`       | 重启间隔秒数       | 防止疯狂重启                                     |
| `Environment`      | 环境变量           | 可写多行                                         |
| `EnvironmentFile`  | 从文件读取环境变量 | 敏感信息建议放这里，如密钥、密码                 |
| `LimitNOFILE`      | 最大文件描述符数   | 高并发服务必调，否则报 Too many open files       |
| `TimeoutStartSec`  | 启动超时时间（秒） | 默认 90 秒，服务启动慢需调大                     |
| `TimeoutStopSec`   | 停止超时时间（秒） | 默认 90 秒，服务优雅退出慢需调大                 |

**Type 类型**

| 类型      | 含义                                       | 适用场景                                |
| :-------- | :----------------------------------------- | :-------------------------------------- |
| `simple`  | 认为 ExecStart 启动的进程就是主进程        | **默认值**，适合 Flask、Node 等前台进程 |
| `forking` | ExecStart 启动的进程会 fork 出子进程后退出 | 适合 Nginx 这种经典 daemon 服务         |
| `oneshot` | 执行一次就退出                             | 适合一次性初始化任务                    |
| `notify`  | 服务启动完成后会通知 systemd               | 需要服务代码配合                        |

> 如果用 `simple` 启动 Nginx，Nginx 会一直卡着，因为 Nginx 默认是 daemon 模式（fork 出子进程后父进程退出）。正确的是 `Type=forking`。类型和服务的运行方式不匹配，服务就会启动失败或行为异常

**Restart 策略**

| 值            | 含义                           |
| :------------ | :----------------------------- |
| `no`          | 不自动重启（默认）             |
| `on-failure`  | 只有异常退出时才重启           |
| `always`      | 无论什么原因退出都重启         |
| `on-abnormal` | 只有信号导致退出时才重启       |
| `on-success`  | 只有正常退出（退出码 0）才重启 |

> `always` 和 `on-failure` 的区别在于：如果你手动 `systemctl stop` 服务，`always` 会把它拉起来，`on-failure` 不会。**生产环境用 `on-failure` 更多**，避免你想停服务却停不掉

**[Install] 段**

| 参数         | 作用                                             |
| :----------- | :----------------------------------------------- |
| `WantedBy`   | 指定挂到哪个 target 下，通常 `multi-user.target` |
| `RequiredBy` | 强依赖版本，用的少                               |

> `WantedBy=multi-user.target` 是命令行模式的标准写法。如果你不写 `[Install]` 段，`systemctl enable` 会报错，服务无法设置开机自启

> [!NOTE]
>
> 改完 unit 文件**必须** reload，systemd 不会自动检测

#### 案例

1. 自己写的 unit 文件启动失败

**现象：**服务起不来

**常见原因：**

- `ExecStart`    路径写错或不是绝对路径
- `Type` 和实际进程行为不匹配（如 Nginx 用 simple）
- `User` 指定的用户不存在或无权限
- 环境变量缺失
- 改完没执行 `daemon-reload`

**排查：**

```shell
# 看状态
systemctl status myapp

# 看日志
journalctl -u myapp -n 50 --no-pager

# 检查 unit 文件语法
systemd-analyze verify /etc/systemd/system/myapp.service
```

---

---

[^1]:PAM 是 Linux 的认证框架（具体详情见下方）

| 模块               | 控制对象       | 作用                                                |
| :----------------- | :------------- | :-------------------------------------------------- |
| `pam_wheel.so`     | `su`           | 限制只有 `wheel` 组才能 `su` 到 root                |
| `pam_shells`       | `su` / `login` | 检查用户登录时指定的 shell 是否存在于 `/etc/shells` |
| `pam_timestamp.so` | `sudo`         | 管理 sudo 密码缓存时间（5分钟默认）                 |
| `pam_unix.so`      | `su` / `sudo`  | 验证用户名和密码                                    |

