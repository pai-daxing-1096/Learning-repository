# Shell脚本学习笔记

---

### 1.编写规范

#### (1)脚本后缀名规范

Shell脚本就是一个文本文件，后缀名建议使用`.sh`结尾

#### (2)首行格式规范

首行需要设置Shell解析器的类型，语法：

``````shell
#!/usr/bin/env bash
``````

>含义：设置当前shell脚本文件采用bash解析器运行脚本代码

#### (3)注释格式

单行注释：

``````shell
# 注释内容
``````

多行注释：

``````shell
:<<!
# 注释内容1
# 注释内容2
!
``````

#### (4)单双引号的使用

```
''
```

> 内部所有字符都保持原样**不会进行任何变量替换、命令替换、转义处理**
>
> 即便在命令执行到此行时也不会直接解释里面的内容，而是等后续得到输入时开始解释

```
""
```

> 会进行**变量替换**和**命令替换**，但保留其他字符原样
>
> 在命令执行到此行时会直接解释里面的内容

#### (5)连续创建多行文本块

```shell
命令 << 'END'
内容
END
```

#### (6)核心符号用途表

| 符号     | 名称与主要作用                                               | 典型使用场景                           | 示例                              |
| :------- | :----------------------------------------------------------- | :------------------------------------- | :-------------------------------- |
| **`&`**  | **1. 后台运行** 放在命令**末尾**，使其在后台运行。           | 启动不需要阻塞终端的前台服务。         | `python3 app.py &                 |
|          | **2. 重定向中的“引用”** `&1` 表示“文件描述符1的**目的地**”。 | 在重定向中合并输出流。                 | `2>&1`                            |
| **`$`**  | **变量取值** 放在变量名**前**，获取变量的**值**。            | 使用已定义的变量。                     | `echo $LOG_DIR`                   |
| **`$ ()` | **命令替换** **执行**括号内的命令，并用其**输出结果替换**整个`$()`。 | 获取动态值（日期、命令输出、计算等）。 | `LOG_FILE=”…$(date +%Y%m%d).log”` |

---

### 2.脚本文件常见的三种解析方式

#### (1) sh解析器执行方式

使用sh命令执行脚本文件，本质是使用Shell解析器运行脚本文件

``````shell
sh 脚本文件
``````

#### (2) bash解析器执行方式

使用bash命令执行脚本文件，本质是使用Shell解析器运行脚本文件

``````shell
bash 脚本文件
``````

#### (3) 仅路径执行方式

执行当前目录下的脚本文件

``````shell
./脚本文件
``````

>脚本文件自己执行需要所需权限，否则无法执行

---

### 3.自定义变量的增删改查

#### (1)自定义变量

语法：

``````shell
变量名="值"
``````

变量定义规则

- 变量名称可以有字母，数字和下划线组成，但是不能以数字开头
- 等号两侧不能有空格
- 在bash环境中，变量的默认类型都是字符串类型，无法直接进行数值运算
- 变量的值如果有空格，必须使用双引号括起来
- 不能使用Shell的关键字作为变量名称

> 局部变量仅在当前 Shell 进程中有效，子 Shell 无法访问，退出脚本后变量失效

#### (2)自定义变量的查询/调用

语法：

``````shell
$变量名
``````

``````shell
${变量名}
``````

>区别：
>
>加`{}`的用法可用于拼接字符串，例如`${name}123`

#### (3)自定义变量的修改

语法：

``````shell
变量名="新值"
``````

> 直接替换即可

#### (4)自定义变量的删除

语法：

``````shell
unset 变量名
``````

#### (5)自定义局部变量

语法：

```shell
local 函数名=值
```

> 声明该函数为局部变量

---

### 4.自定义常量

介绍：

> 就是变量设置值以后不可以修改的变量叫常量，也叫只读变量

语法：

``````shell
readonly 变量名="值"
``````

注意：

> 只读变量(常量)无法用 `unset` 删除，只能关闭当前 Shell 或重启终端
>
> 除此之外，常量同样分“局部常量”和“全局常量”，和变量相比只是有“只读”区别

---

### 5.变量默认值

**变量默认值语法**是 Shell 中用于处理变量可能未设置或为空时的便捷语法。它可以在变量为空时提供一个备用值，避免脚本因空变量而出错

语法：

``````shell
${变量:-默认值}
``````

- **作用**：如果变量未设置或为空，则返回默认值，但**不改变**原变量

语法：

``````shell
${变量:=默认值}
``````

- **作用**：如果变量未设置或为空，则**设置变量为默认值**并返回

语法：

```shell
${变量:+替代值}
```

- **作用**：如果变量已设置或不为空，则返回替代值

语法：

```shell
${变量:?错误信息}
```

- **作用**：如果变量未设置或为空，输出错误并退出

---

### 6.自定义全局变量\环境变量

```shell
set
```

> 输出全部变量，包括全局变量、局部变量

```shell
env
```

> 显示全部全局变量

```shell
declare
```

> 输出所有变量，如同`set`命令

```shell
export
```

> 显示和设置环境变量值

**父子Shell环境**：

> 如果在A.sh脚本中调用了B.sh脚本，则认为A.sh脚本为B.sh脚本的父Shell环境，B.sh就是子Shell环境

语法：

``````shell
export name="值"
``````

> 全局变量可以在声明处及其子进程中被读取，无法被父进程读取，此外，子进程可以修改全局变量，但是将无法被父进程所读取，只能被其或其子进程读取

#### (1)全局变量(用户级)

```shell
~/.bash_profile
```

> 该文件为Bash登录shell的用户级配置文件，它位于用户主目录下，可使用<u>`export 变量名=值`</u>将变量设置为**环境变量**
>
> 修改 `.bash_profile` 只会影响**当前用户**

```shell
~/.bashrc
```

> `.bashrc` 是 **别名（Aliases）、函数、shell选项和交互式配置** 的主要存放地
>
> 可使用`alias 别名='命令或路径'`设置别名

#### (2)全局变量(系统级)\系统变量

```shell
/etc/profile
```

> 该文件所有用户的Bash登录shell的用户级配置文件，可使用<u>`export 变量名=值`</u>将变量设置为**环境变量**
>
> 修改 `profile` 会影响**所有用户**

``` shell
/etc/profile.d/
```

> 可在此文件下创建自定义配置文件，与上面的方法配置的效果相同**(推荐)**

---

### 7.特殊变量

```shell
$0
```
> 获取Shell脚本名以及脚本路径
```shell
$n
```
> 获取Shell脚本的第n个参数，n在1~9之间，大于9则需要写`${10}$`,参数使用空格隔开
```shell
$#
```
> 获取执行的Shell脚本的参数总个数
```shell
$*
```
> 获取Shell脚本的所有参数，不加引号等同于`$#`，加上引号`“$*”`的作用时接收所有为单个字符串的参数
>
> 即：将所有参数从整体上看作一份数据
```shell
$@
```
> 不加引号效果同`$#`，加引号`$@`则接受所有参数独立的字符串
>
> 即：将每一份参数单独看作一份数据

---

### 8.特殊状态变量

```
$?
```

>取得上一次命令的状态返回值，0为正常执行，1~255则为报错

```
$$
```

>获取当前Shell脚本的进程号

```
$!
```

>获得上一个后台进程的PID

```
$_
```

> 获取上一个被执行命令取得的最后一个参数

---

### 9.基础变量操作

#### (1). 变量定义与赋值

bash

```
# 基本赋值（等号两边不能有空格！）
NAME="value"          # 字符串
COUNT=10              # 数字
RESULT=$(command)     # 命令输出
RESULT=`command`      # 旧式命令替换

# 数组赋值
ARRAY=("value1" "value2" "value3")  # 索引数组
declare -A DICT=(["key1"]="val1" ["key2"]="val2")  # 关联数组

# 只读变量（常量）
readonly PI=3.14
declare -r MAX=100
```



#### (2). 变量引用与取值

bash

```
VAR="hello"
echo $VAR          # 简单引用（可能有问题）
echo ${VAR}        # 安全引用（推荐！）
echo "${VAR}"      # 带引号引用（最安全）

# 为什么需要花括号？
NAME="Tom"
echo "Hi $NAMEson"    # 错误：尝试读取$NAMEson变量
echo "Hi ${NAME}son"  # 正确：输出"Hi Toms on"
```



#### (3). 特殊变量

bash

```
$0      # 脚本名称
$1-$9   # 位置参数（第1-9个参数）
$#      # 参数个数
$@      # 所有参数（每个单独）
$*      # 所有参数（作为一个整体）
$$      # 当前进程PID
$?      # 上条命令的退出码（0=成功）
$!      # 最后一个后台进程PID
$_      # 上条命令的最后一个参数
```



### 10.数组操作完全指南

#### (1). 数组定义与初始化

bash

```
# 四种定义方式
PACKAGES=("tmux" "nginx" "htop")           # 直接赋值
SERVICES=([0]="nginx" [1]="mysql" [2]="redis")  # 指定索引
NAMES=()                                   # 空数组
FRUITS=(apple banana cherry)               # 空格分隔

# 动态添加元素
PACKAGES+=("vim")          # 末尾添加
PACKAGES=("wget" "${PACKAGES[@]}")  # 开头添加
```



#### (2). 数组访问与遍历

bash

```
ARRAY=("first" "second" "third")

# 访问单个元素
echo ${ARRAY[0]}      # 第一个元素：first
echo ${ARRAY[-1]}     # 最后一个元素：third

# 获取所有元素
echo ${ARRAY[@]}      # 所有元素（作为列表）
echo ${ARRAY[*]}      # 所有元素（作为单个字符串）

# 获取数组长度
echo ${#ARRAY[@]}     # 元素个数：3
echo ${#ARRAY[0]}     # 第一个元素的长度：5

# 遍历数组 - 三种方式
# 1. 遍历值（你用的方式）
for item in "${ARRAY[@]}"; do
    echo "值: $item"
done

# 2. 遍历索引
for i in "${!ARRAY[@]}"; do
    echo "索引 $i: ${ARRAY[$i]}"
done

# 3. C风格遍历
for ((i=0; i<${#ARRAY[@]}; i++)); do
    echo "元素 $i: ${ARRAY[$i]}"
done
```



#### (3). 数组切片与操作

bash

```
ARRAY=("a" "b" "c" "d" "e" "f")

# 切片
echo ${ARRAY[@]:1:3}     # 从索引1开始取3个：b c d
echo ${ARRAY[@]::3}      # 前3个元素：a b c
echo ${ARRAY[@]: -3}     # 最后3个：d e f

# 搜索与替换
echo ${ARRAY[@]/b/B}     # 替换第一个b为B
echo ${ARRAY[@]//b/B}    # 替换所有b为B

# 删除元素
unset ARRAY[2]           # 删除索引2的元素（c）
ARRAY=(${ARRAY[@]})      # 重新索引
```



### 11.字符串操作

#### (1). 长度与子串

bash

```
STR="Hello World"

# 获取长度
echo ${#STR}            # 11

# 子串提取
echo ${STR:0:5}         # Hello（从0开始取5个）
echo ${STR:6}           # World（从6开始到末尾）
echo ${STR: -5}         # World（最后5个，注意空格）

# 删除匹配内容
FILE="backup.tar.gz"
echo ${FILE%.*}         # 删除最短的后缀：backup.tar
echo ${FILE%%.*}        # 删除最长的后缀：backup
echo ${FILE#*.}         # 删除最短的前缀：tar.gz
echo ${FILE##*.}        # 删除最长的前缀：gz
```



#### (2). 查找与替换

bash

```
PATH="/usr/local/bin:/usr/bin:/bin"

# 查找替换
echo ${PATH/bin/BIN}    # 替换第一个bin：/usr/local/BIN:/usr/bin:/bin
echo ${PATH//bin/BIN}   # 替换所有bin：/usr/local/BIN:/usr/BIN:/BIN

# 条件替换
VAR=""
echo ${VAR:-"默认值"}   # 如果VAR为空/未设置，用默认值
echo ${VAR:="默认值"}   # 如果VAR为空/未设置，赋值并返回
echo ${VAR:+"有值"}     # 如果VAR有值，返回"有值"
echo ${VAR:?"错误"}     # 如果VAR为空，显示错误并退出
```



#### (3). 大小写转换

bash

```
STR="Hello Bash"

echo ${STR^^}           # 转大写：HELLO BASH
echo ${STR,,}           # 转小写：hello bash
echo ${STR^}            # 首字母大写：Hello bash
echo ${STR,}            # 首字母小写：hello Bash
```



### 12.变量高级技巧

#### (1). 变量间接引用

bash

```
NAME="user"
user="张三"

echo ${!NAME}          # 输出"张三"（间接引用）
```



#### (2). 命名空间与局部变量

bash

```
# 局部变量（函数内部）
func() {
    local var="局部变量"
    echo $var
}

# 环境变量（子进程可继承）
export GLOBAL_VAR="全局变量"
declare -x ANOTHER_GLOBAL="另一个全局变量"
```



#### (3). 变量属性

bash

```
declare -i NUMBER=10     # 整数类型（可进行算术运算）
declare -r CONST=100     # 只读（常量）
declare -a ARRAY         # 数组
declare -A MAP           # 关联数组
declare -x EXPORT_VAR    # 导出为环境变量
declare -l lower="HELLO" # 赋值时转为小写：hello
declare -u upper="hello" # 赋值时转为大写：HELLO
```

### 13.Shell子串

#### (1)bash一些基础内置命令

```shell
echo [-n] [-e] [\n] [\r] [\t] [\b]
```

> 可以使用`echo`命令输出指定内容

- `-n`   不换行输出
- `-e`   解析字符串中的特殊符号
- 特殊符号：
	- `\n`   换行
	- `\r`   回车
	- `\t`   制表符(四个空格)
	- `\b`   退一格


```shell
printf "[\n] [\r] [\t] [\b]"
```

> 可以使用`printf`命令输出指定内容,与`echo`命令的区别是使用特殊符号时不需要使用参数`-e`

- `\n`   换行
- `\r`   回车
- `\t`   制表符(四个空格)
- `\b`   退一格

```shell
eval
```

> 将字符串作为命令执行，可同时执行多个命令

### 14.逻辑运算符

```shell
;
```

> 用于连续执行多个命令，按顺序执行，上一个命令**无论是否正确**都会继续执行

```shell
&&
```

> 条件判断中表示：且，用于连续执行多个命令，按顺序执行，上一个命令**成功**才会执行下一个

```shell
||
```

> 条件判断中表示：或，用于连续执行多个命令，按顺序执行，上一个命令**失败**才会执行下一个

```shell
exec
```

> 不创建子进程，执行后续命令，且命令执行完毕后自动执行`exit`命令，常用于执行完命令后退回到上一个用户

---

### 15.运算符

```shell
$((运算式)) 或 $[运算式]
```

> `$((运算式))`支持所有C语言风格的所有算术运算，`$[运算式]`为老写法，现已不建议使用，且使用双括号后不应当再里面使用`$`，因为其会自动解析变量名

- `+`  加
- `-`   减
- `*`   乘
- `**`   幂运算
- `>`   小于
- `<`   大于
- `<=`   小于等于
- `>=`   大于等于
- `i++`   自增
- `i--`   自减
- `&`   与
- `|`   或
- `^`   异或
- `&&`   逻辑与
- `||`   逻辑或

```shell
expr [+] [-] [\*] [/] [%]
```

- `+`  加
- `-`   减
- `\*`   乘(此写法为转义符`\`加乘号`*`)
- `/`   除
- `%`   取余

> [!IMPORTANT]
>
> 运算符间需要有空格

---

### 16.条件判断

语法：

```shell
[ 条件1 判断条件 条件2 ]
```

#### (1).常用判断条件：

- `=`   等于，用于**字符串**比较
- `-lt`   小于
- `-le`   小于等于
- `-eq`   等于，用于**数字**比较
- `-gt`   大于
- `-ge`   大于等于
- `-ne`   不等于
- `&&`   且
- `||`   或
- `!`   非
- `=~`   正则pi'pei

例：

```shell
[ 22 -lt 23 ]
```

> 求22是否小于23，若正确，则可以使用特殊状态变量`$?`查询得到返回值为0，若不正确则返回值为1

例：

```shell 
[ 条件1 ] &&\|| [ 条件2 ]
```

```
[[ 条件1 &&\|| 条件2 ]]
```

> [!IMPORTANT]
>
> 判断条件`&&`和`||`不可以直接使用方括号直接括起来使用

#### (2)`[]` vs `[[]]` 的区别

- **`[]`**：老的test命令，兼容性好但功能有限
- **`[[]]`**：Bash的增强版，功能更强大更安全

| 特性           | `[ ]` (test)          | `[[ ]]` (Bash)           | 示例                         |
| :------------- | :-------------------- | :----------------------- | :--------------------------- |
| **字符串比较** | `=` 或 `==`           | `==` 或 `=`              | `[ "$a" = "$b" ]`            |
| **模式匹配**   | ❌ 不支持              | ✅ 支持                   | `[[ $file == *.txt ]]`       |
| **正则表达式** | ❌ 不支持              | ✅ 支持                   | `[[ $string =~ ^[0-9]+$ ]]`  |
| **逻辑操作符** | `-a` (and), `-o` (or) | `&&`, `||`               | `[[ $a && $b ]]`             |
| **括号分组**   | 需要转义 `\( \)`      | 直接使用 `( )`           | `[[ ($a == 1 && $b == 2) ]]` |
| **字符串为空** | `-z`, `-n`            | `-z`, `-n`               | 相同                         |
| **路径名扩展** | ✅ 会扩展              | ❌ 不扩展                 | 更安全                       |
| **单词拆分**   | ✅ 会拆分              | ❌ 不拆分                 | 更安全                       |
| **比较运算符** | `-lt`, `-gt` 等       | `-lt`, `-gt` 或 `<`, `>` | `[[ $a < $b ]]`              |

#### (2).按照文件权限进行判断：

- `-r`   阅读权限
- `-w`   写入权限
- `-x`   执行权限

例：

```shell
[ -r /home/pai/text.txt ]
```

> 查询text.txt文件是否有阅读权限，若有权限，则可以使用特殊状态变量`$?`查询得到返回值为0，若无权限则返回值为1

#### (3).按照文件类型进行判断

- `-f`   文件存在并且是常规文件
- `-e`   文件存在
- `-d`   文件存在并且是目录(文件夹)

例：

```shell
[ -e /home/pai/text.txt ]
```

> 查询text.txt文件是否存在，若存在，则可以使用特殊状态变量`$?`查询得到返回值为0，若不存在则返回值为1

#### (4).[] 的真值表

| 表达式          | 含义           | 返回值（0=真, 1=假） |
| :-------------- | :------------- | :------------------- |
| `[ ]`           | 空表达式       | 1（假）              |
| `[ "" ]`        | 测试空字符串   | 1（假）              |
| `[ "text" ]`    | 测试非空字符串 | 0（真）              |
| `[ -n "text" ]` | 字符串非空     | 0（真）              |
| `[ -z "text" ]` | 字符串为空     | 1（假）              |
| `[ 1 -eq 1 ]`   | 数字相等       | 0（真）              |
| `[ -f file ]`   | 文件存在       | 取决于文件           |

> [!IMPORTANT]
>
> 方括号与条件间同样需要有空格

---

### 17.流程控制

#### (1).交互式读取用户输入

语法：

```shell
read -p "提示信息" 变量名
```

| 选项 | 作用                   | 示例                              |
| :--- | :--------------------- | :-------------------------------- |
| `-p` | 显示提示信息           | `read -p "输入: " var`            |
| `-s` | 静默模式（不显示输入） | `read -s -p "密码: " pass`        |
| `-t` | 超时时间（秒）         | `read -t 5 -p "5秒内输入: "`      |
| `-n` | 读取指定字符数         | `read -n 1 -p "输入一个字符: "`   |
| `-r` | 禁止反斜杠转义         | `read -r -p "输入路径: "`         |
| `-a` | 读取为数组             | `read -a array -p "输入多个值: "` |

#### (2).if判断

语法：

```shell
if [ 条件判断 ];then   #[ 条件判断1 ] &&\|| [ 条件判断2 ]		[ 条件判断1 -a 条件判断2 ]
	程序
elif [ 条件判断 ];then
	程序
else
	程序
fi
```

#### (3).case语句

语法：

```shell
case $变量名 in
"值1")								#可以使用1[0-9]|2[0-9]进行模式匹配
如果变量的值等于值1，则执行程序1 
;;
"值2")
	如果变量的值等于值2，则执行程序2
;;
"值3")
	如果变量的值等于值3，则执行程序3
;;
*)
	如果变量的值都不等于以上的值，则执行程序4
;;
esac
```

**模式匹配语法**：

- `|`：或操作

- `*`：任意字符
- `?`：单个字符
- `[abc]`：字符集
- `[a-z]`：字符范围

#### (4).for循环

语法：

```shell
for ((初始值;循环控制条件;变量变化))
do
	程序
done
```

```shell
for 变量(i、j、k...) in 值1 值2 值3 ... 值n
do
	程序
done
```

#### (5).while循环

语法：

```shell
while[ 条件判断 ]
do
	程序
done
```

#### (6)特殊用法

```
while read -r line;do
	...
done <<<"文件"/< <(...)
     Here String  进程替换
     避免在子进程中运行
```



---

### 18.系统函数

#### (1).basename函数

语法：

```shell
basename [string/pathname] [suffix]
```

> 删除`/`前面的目录，把文件名显示出来，并且会把指定的`suffix`后缀删除

例：

```shell
basename ~/text.txt .txt
text
```

#### (2).dirname函数

语法：

```shell
dirname 文件绝对路径
```

> 删除文件名，把目录显示出来

---

### 19.自定义函数

语法：

```shell
[ function ] 函数名[()]
{
	函数体
	[return int;]		#返回语句
}
```

---

### 20.正则表达式

| 符号                        | 作用                                                         |
| --------------------------- | ------------------------------------------------------------ |
| ^                           | 用于模式的最左侧。例如“^hello”，匹配以hello单词为开头的行    |
| $                           | 用于模式的最右侧。例如“hello$”，匹配以hello单词为结尾的行    |
| ^$                          | 组合符，表示匹配空行                                         |
| .                           | 匹配任意一个且只有一个的字符，不能匹配空行，主要用于“代替”，例如“.s”，匹配任何“莫字母＋s”的组合，也可以找出所有空行以外的内容 |
| \                           | 转义字符，让有特殊含义的字符现出原形，恢复其本意             |
| *                           | 匹配前面一个字符0次或者多次，例如“ab*c”,匹配出现过0次或者多次b的行，可以是abbc(两次)，abbbbc(三次) |
| .*                          | 贪婪匹配，匹配所有内容，包括空格，例如“a.*n”,匹配所有a开头n结尾的字符串，不只包括该词，还包括该行前面的所有字符 |
| ^.*                         | 从开头到结尾的所有字符，例如“^.*n”,匹配从开头开始到n的字符串 |
| .*$                         | 从开头到结尾的所有字符,例如“.*$n”,匹配从结尾开始到n的字符串  |
| [abc]                       | 表示匹配中括号中任意一个字符                                 |
| [[:space:]]                 | 空格匹配                                                     |
| [^abc]                      | 与上面的作用相反，是把匹配的东西过滤掉                       |
| 单词边界符号(仅限grep和sed) | 解释                                                         |
| \\<                         | 单词左边界                                                   |
| \\>                         | 单词右边界                                                   |
| \\b                         | 单词边界(上面二者的简写)                                     |

---

### 20.Shell工具

单双引号使用场景：

| 场景                 | 推荐用法             | 原因           |
| :------------------- | :------------------- | :------------- |
| **固定模式匹配**     | `grep 'pattern'`     | 安全，无需转义 |
| **包含正则特殊字符** | `grep '^abc$'`       | 防止Shell解释  |
| **使用Shell变量**    | `grep "$var"`        | 需要变量扩展   |
| **awk字段引用**      | `awk '{print $1}'`   | `$1`由awk解释  |
| **传递变量到awk**    | `awk -v a="$var"`    | 最安全的方式   |
| **sed简单替换**      | `sed 's/old/new/'`   | 避免意外扩展   |
| **sed使用Shell变量** | `sed "s/$old/$new/"` | 需要变量替换   |
| **包含单/双引号**    | 混合使用或转义       | 正确处理引号   |

#### (1)grep

语法：

```shell
grep [参数] "匹配模式" 文件名称
```

| 参数选项    | 解释说明                                               |
| ----------- | ------------------------------------------------------ |
| -v          | 排除匹配结果                                           |
| -n          | 显示匹配结果与行号                                     |
| -i          | 不区分大小写                                           |
| -c          | 只统计匹配的行数                                       |
| -E          | 使用egrep命令(拓展)                                    |
| –color=auto | 为grep过滤结果添加颜色                                 |
| -w          | 只匹配过滤的单词                                       |
| -o          | 只输出匹配的内容                                       |
| -l          | 检查哪些文件里面存在所匹配成功的信息，并打印出那些文件 |
| -q          | 安静模式，只输出状态代码（0为成功，1~255为错误）       |

| 符号(拓展) | 作用                                                         |
| ---------- | ------------------------------------------------------------ |
| ?          | 与*相似，但是其只会匹配出现0次或1次的                        |
| [:/]+      | 匹配括号内的“：”或“/”一次或多次                              |
| +          | 与*相似，但是其只会匹配出现1次或多次的                       |
| \|         | 表示或，例如“a\|x”,匹配带有a或者x的行                        |
| ()         | 将多个字符绑定在一起，当作一个整体使用                       |
| \n         | 引用被()绑定的字符，\1表示引用第一个，重点是其模式和被引用的模式完全相同，前者匹配到的是hello，后者不会匹配到hakko，而必须是hello |
| a{n,m}     | 匹配次数，重复前一个字符各种次数，n代表最少几次，m代表最多几次，例如：a{1，3}，匹配a出现1次到3次的字符串 |
| \s         | 匹配空白字符                                                 |
| \S         | 匹配非空白字符                                               |
| \w         | 匹配数字                                                     |
| \d         | 匹配单词字符                                                 |

#### (2)cut

语法：

```shell
cut [-d "分隔符"] [-f 列号] 文件名称
```

- `-f`   提取几列
- `-d`   按照指定分隔符分隔列
- `列号-`  取该列后的字符串
- `-列号`   取该列前的字符串

#### (3)sed

语法：

```shell
sed [参数] "内置命令" 文件名
```

| 参数选项 | 解释                                            |
| -------- | ----------------------------------------------- |
| -n       | 取消默认的sed输出，常与内置命令p一起使用        |
| -i       | 直接将内容写入文件，不用-i，sed修改的是内存数据 |
| -e       | 多次编辑，替代管道符，用于内置命令的前方        |
| -r       | 支持正则扩展                                    |

| 命令格式      | 解释                  |
| ------------- | :-------------------- |
| `na\text`     | 在第n行**后**插入文本 |
| `ni\text`     | 在第n行**前**插入文本 |
| `nd`          | 删除第n行             |
| `/pattern/d`  | 删除匹配模式的行      |
| `n,ms/原/新/` | 替换第n到m行内容      |
| `ns/原/新/`   | 替换第n行内容         |
| `-n 'n,mp'`   | 打印n到m行            |
| `s/原/新/g`   | **全局**替换所有匹配  |
| \n            | 在替换文本中表示换行  |

| 范围           | 解释                                                         |
| -------------- | ------------------------------------------------------------ |
| 空地址         | 全文处理                                                     |
| 单地址         | 指定文件某一行                                               |
| /要匹配的内容/ | 被模式匹配到的每一行                                         |
| 范围区间       | 10，20 十到二十行，10，+5第十行向下5行，/pattern1/，/pattern2/ |
| 步长           | 1(起始行)~2(步长)，表示1、3、5、7、9奇数行， 2~2表示2、4、6、8、10偶数行，前者指从哪行开始，后者指步长为几 |

> 该命令不会更改原文件，除非使用重定向符写入或替换
>
> 可以使用正则表达式

#### (4)awk

语法：

```shell
awk 参数 '模式{动作 内置变量1,内置变量2}' 文件
```

> 动作常用`print`或`printf`
>
> 例如:`awk '{print $n}' 文件名`表示输出文本第n列的信息

| 内置变量 | 解释                                                        |
| -------- | ----------------------------------------------------------- |
| $n       | 指定分隔符后，当前记录的第n个字符串(第几列)                 |
| $0       | 完整的输入记录                                              |
| FS       | 字段分隔符，默认是空格                                      |
| NF       | 分隔后，当前行一共有多少个字段，可以$(NF-n)来从后向前定位行 |
| NR==n    | 当前记录数，n为第n行，也可以放进{}当行号用                  |
| man awk  | 更多内置变量可以man手册查看                                 |

| 参数         | 解释                                                         |
| ------------ | ------------------------------------------------------------ |
| -F ’ 分隔符‘ | 指定分割字段符(默认为空格)                                   |
| -v           | 定义或修改一个awk内部的变量，可以用FS=“：”换分隔符也可以OFS=“—”切换输出分隔符 |
| -f           | 从脚本文件中读取awk命令                                      |

| 内置变量 | 解释                                                     |
| -------- | -------------------------------------------------------- |
| FS       | 输入字段分隔字符，默认位空白字符（空格）                 |
| OFS      | 输出字段分隔字符，默认位空白字符（空格）                 |
| RS       | 输入记录分隔符（输出换行符），指定输入时的换行符         |
| ORS      | 输出记录分隔符（输出换行符），输出时用指定符号代替换行符 |
| NF       | 指代当前字段的个数，字段数量                             |
| NR       | 行号，当前所处理文本行的行号（可作为模式使用）           |
| FNR      | 各文件分别计数的行号                                     |
| FLENAME  | 当前文件名                                               |
| ARGC     | 命令行参数的个数                                         |
| ARGV     | 数组，保存的时命令行所给定的各参数                       |

printf的格式化输出

| 格式化输出 | 解释                                     |
| ---------- | ---------------------------------------- |
| %c         | 显示字符的ASCII码                        |
| %d%i       | 十进制整数                               |
| %e%E       | 科学计数法显示数值                       |
| %f         | 显示浮点数                               |
| %g%G       | 以科学计数法的格式或浮点数的格式显示数值 |
| %s         | 显示字符串                               |
| %u         | 无符号整数                               |
| %%         | 显示%自身                                |

| printf修饰符 | 解释               |
| ------------ | ------------------ |
| -            | 左对齐，默认右对齐 |
| +            | 显示数值符号 “%+d” |

| 模式       | 解释                                                         |
| ---------- | ------------------------------------------------------------ |
| BEGIN      | 表示处理文本前需要进行什么操作，所以BEGIN{printf ….},后面跟着的这个只会执行一次 |
| END        | 表示处理文本后需要进行什么操作，所以END{printf ….},后面跟着的这个只会执行一次 |
| >、<、==等 | 和c语言的逻辑判断一样                                        |
| ~          | 进行正则匹配                                                 |
| !~         | 进行非正则匹配                                               |

awk与正则表达式

语法：

```shell
awk '/模式1/，/模式2/动作' 文件名
```

> 和sed的用法一样,不过这里是放在“模式”的位置

```shell
systemctl list-units --type=service | grep -v -E "UNIT|systemd|kdump" | awk 'BEGIN{printf "%-4s %-45s %-10s %-10s %-10s %s\n","#","UNIT","LOAD","ACTIVE","SUB","DESCRIPTION"}{printf "%-4s %-45s %-10s %-10s %-10s %s\n",NR,$1,$2,$3,$4,$5}' | awk 'NR<=36{print $0}'
```

> 第一次尝试混着用，当然我知道写的跟坨答辩一样，限制太多了，不过这也相当于我的第一次实战了，纪念一下吧

---

### 21.定时任务

#### (1)crontab命令

> 当系统管理员 (root) 或是普通用户创建了需要定期执行的任务，可以使用 crontab 命令配置，crond 服务在启动时，会每分钟查看 /var/spool/cron 路径下以系统用户名命名的定时任务文件，以确定是否有需要执行的任务

语法：

```shell
*   *   *   *    *    需要执行的任务/脚本
分  时  日   月   周
```

| 参数      | 解释                                                   | 使用示例          |
| --------- | ------------------------------------------------------ | ----------------- |
| -l        | list查看定时任务                                       | crontab -l        |
| -e        | edit编辑定时任务，建议手动编辑                         | crontab -e        |
| -i        | 删除定时任务，提示用户确认删除，避免出错               | crontab -i        |
| -r        | 删除定时任务，移除/var/spool/cron/username文件，全没了 | crontab -r        |
| -u 用户名 | 指定用户执行任务， root 可以管理普通用户计划任务       | crontab -u pai -l |

| 特殊符号 | 解释                                                         |
| -------- | ------------------------------------------------------------ |
| *        | 表示"每''，既如 23 cmd 表示每月每周每日的 23 ： 00 整点执行命令 |
| -        | 减号表示时间范围分隔符，如17-19，代表每天的17、18、19点      |
| ,        | 表示分割时间段，如30 17，18，19 *cmd 表示每天的17、18、19点半执行命令 |
| /n       | n表示可以整除的数字，每隔n的单位时间，如每隔10分钟表示/10 *cmd |

